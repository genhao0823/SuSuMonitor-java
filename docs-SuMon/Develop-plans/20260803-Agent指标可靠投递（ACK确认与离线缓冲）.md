# 20260803-Agent 指标可靠投递（ACK 确认与离线缓冲）

**状态**：已实施 M1/M2，文档与验证收口中
**日期**：2026-08-03
**前置提交**：`773fc4d`（Server ACK）与 `7db29b6`（Agent 本地 FIFO）

## 1. 目标与范围

将 Agent 指标上报由“WebSocket 写入成功即视为成功”的在线 best-effort 模式，收口为“服务端入口事务接受并回传 `metrics.ack` 后才删除本地待确认项”的有界可靠投递。

本轮不引入新的消息中间件、跨 JVM 状态、数据库迁移或浏览器端投递状态页面；指标进入 Java 后的 Metrics Outbox 与 RabbitMQ 告警链路仍沿用既有职责边界。

## 2. 协议与参数

### 2.1 `metrics.ack`

Server 在 `MetricsService.report(...)` 成功返回后回传：

```json
{
  "type": "metrics.ack",
  "message_id": "<原 metrics.report UUID>",
  "timestamp": "<UTC ISO-8601>",
  "payload": {"server_id": 1, "collected_at": "<UTC ISO-8601>"}
}
```

它只确认 Metrics 入口事务（包含重复 `message_id` 的幂等接受）已成功；不确认 Outbox 发布、RabbitMQ、`metrics.update` 或异步告警评估。

### 2.2 Agent 环境变量

| 参数 | 默认值 | 传递链路 | 约束 |
|---|---:|---|---|
| `SUSUMONITOR_METRICS_BUFFER_PATH` | `/var/lib/susumonitor/metrics-buffer.json` | env → `config.Config` → `metricbuffer.Open` | 必须是 clean absolute path |
| `SUSUMONITOR_METRICS_BUFFER_MAX_ENTRIES` | `720` | env → `config.Config` → Queue capacity | 1–100000；默认对应 5 秒采集约 1 小时 |

队列文件和父目录分别以 `0600`、`0700` 创建；安装脚本预建 `/var/lib/susumonitor` 并设为 `root:root/0700`。

## 3. 状态机和数据流

```text
collector
  → Reporter 创建带固定 UUID 的完整 metrics.report
  → Queue 原子写入 JSON snapshot（FIFO）
  → 唯一 in-flight sender 写入 WebSocket
  → Server metrics ingress transaction
  → metrics.ack(message_id)
  → Queue 仅匹配 FIFO 头部后原子删除
  → 发送下一条
```

- 断线、写错误或 ACK 丢失：记录仍在磁盘；下一次认证后以**同一 UUID**重放。
- 一个时刻仅发送一个未确认指标，避免服务端 `collected_at` 严格递增约束下的乱序。
- 缓冲满时拒绝**最新**采样并记录错误，保留最旧的可恢复 FIFO 前缀；本轮不静默丢弃。
- 缓冲文件损坏、版本不支持或所属 `server_id` 不匹配时启动失败，不猜测性丢弃待确认数据。
- M2 Agent 的部署依赖 M1 Server（`773fc4d` 或后续版本）；旧 Server 不发 `metrics.ack` 会使队首持续保留，不能将超时伪装成投递成功。

## 4. 文件影响面

- Server：`AgentMessageType`、`AgentWebSocketHandler`、其 JUnit 测试。
- Agent：`internal/metricbuffer`（新增）、`internal/reporter`、`internal/wsclient`、配置、启动、安装和配置样例。
- 协议：`docs-SuMon/Protocol-SuMon/websocket-protocol.md` v1.2。

## 5. 验证矩阵

| 项目 | 结果 | 证据 |
|---|---|---|
| Server `metrics.ack` 成功/失败边界 | PASS | `AgentWebSocketHandlerTests` 10 tests |
| 受影响 Server 契约/metrics/consumer 测试 | PASS | 24 tests |
| 全量 Maven 测试 | PASS | 414 tests |
| Queue 重启恢复、FIFO ACK、满队列保护 | PASS | `internal/metricbuffer` tests |
| Reporter 入队先于发送、ACK 后推进 FIFO | PASS | `internal/reporter` tests |
| WebSocket ACK 专用分发、认证后安全发送 | PASS | `internal/wsclient` tests |
| Go 全量测试、vet、build | PASS | `go test ./...`、`go vet ./...`、`go build ./cmd/susumonitor-agent` |
| 真实断网/重启/服务端 ACK E2E | 未执行 | 后续需隔离环境与支持 `metrics.ack` 的 Server 联合验收 |
| UI/管理端投递积压展示 | 未实施 | 后续独立模块，不能将日志/本地 spool 误称为 UI 可观测性 |

## 6. 备份与回滚

修改前备份位于：

- `C:\Backup\SuSuMonitor\20260803-140000-agent-reliable-delivery\M1-before-change`
- `C:\Backup\SuSuMonitor\20260803-140000-agent-reliable-delivery\M2-before-change`
- `C:\Backup\SuSuMonitor\20260803-140000-agent-reliable-delivery\M3-before-change`

每个目录包含 `SHA256SUMS.txt`、`FILES.txt` 和目标文件副本。恢复时先停止修改过程，再从对应备份路径复制原文件，并以 SHA-256 清单核验。

代码回滚可分别回退到 M1 前提交或 M2 前提交。M1/M2 没有 Flyway 变更；若降级 Server，必须同时停止使用 M2 Agent 或接受其队首将等待 ACK 的设计结果。

## 7. 后续独立模块

1. 为队列增加 ACK 超时、退避和可配置重放节流，并在服务端提供关联的可重试/不可重试 NACK；
2. 通过 Agent 心跳暴露 pending count/bytes/oldest timestamp/drop/dead-letter 状态，再经现有状态接口展示；
3. 在隔离环境执行断网、Agent 重启、重复 ID 幂等、满队列和服务端异常的真实联合 E2E；
4. 在启用多实例/多消费者前继续执行既定并发验证。
