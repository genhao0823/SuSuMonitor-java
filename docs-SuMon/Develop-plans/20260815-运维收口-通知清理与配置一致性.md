# 2026-08-15 质量/运维收口：通知清理与配置一致性

**状态**：✅ 已收口（2026-08-15）
**前置基线**：`main` HEAD `4d5c2a2`
**验收记录**：`docs-SuMon/Develop-log/20260815-运维收口-通知清理与配置一致性.md`

## 一、范围（已确认）

- **A** `alert_notifications` 保留期清理（新功能模块，补齐"只增不删"增长风险）
- **B** Outbox 清理默认开启（`OUTBOX_CLEANUP_ENABLED` false→true）+ poll 默认值对齐（代码 1000ms → yml 实际 200ms）
- **C** 文档滞后回改（rabbitmq-topology 多消费者表述、部署手册 Flyway 版本）
- 孤儿表 `commands`/`ssh_sessions` **保持暂缓**（20260802 决策延续，dev-log 重新留痕）

**技术栈**：零新依赖——Spring Boot 3.4.7 + MyBatis + Flyway（V27）+ JUnit5/Mockito/H2 + MySQL 8.4（可选 IT）。对称复用既有 Cleanup 三件套模式（`AlertRecordCleanupServiceImpl` / `OutboxCleanupServiceImpl` 模板：`@ConditionalOnProperty` + `AtomicBoolean` 防重叠 + `TransactionTemplate` 每批独立事务 + `CleanupResult` + 可注入 cutoff）。

## 二、A 模块：alert_notifications 保留期清理

### A1 Flyway V27
`ALTER TABLE alert_notifications ADD INDEX idx_created_at (created_at);`（V21 仅有 idx_record_channel/idx_next_attempt）

### A2 AppProperties.Alert 5 字段（默认：enabled=true / retention=90 / cron="0 0 3 * * ?" / batch=1000 / maxBatches=100）

### A3 yml alert 块 5 项（`${ALERT_NOTIFICATION_*:默认}`）

### A4 `AlertNotificationCleanupMapper`：`int deleteExpiredBatch(@Param("cutoffTime") LocalDateTime, @Param("limit") int)`

### A5 `AlertNotificationCleanupService/Impl`：`cleanupExpiredNotifications()` + `(LocalDateTime cutoffTime)`，`CleanupResult` 复用

### A6 `AlertNotificationCleanupScheduler`：`@Scheduled(cron = "${susumonitor.alert.notification-cleanup-cron:0 0 3 * * ?}")`

### A7 测试
- `AlertNotificationCleanupMapperMybatisTests`（H2 边界：严格早于 cutoff 删、边界行保留、limit 生效）
- `AlertNotificationCleanupServiceTests`（防重叠、多批循环、batchSize/maxBatches）
- 可选 `AlertNotificationCleanupMySqlValidationIT`（真实 MySQL，failsafe profile）

### A8 文档：本机开发环境配置.md 变量表 5 行

## 三、B 配置与一致性

- B1 `application.yml` `outbox-cleanup-enabled` 默认 false→**true**；`AppProperties.outboxCleanupEnabled` 默认同步 true（只删 `published` 行、30 天保留，与 consume-record 一致）
- B2 `OutboxPublisherScheduler` `@Scheduled` 默认 1000→**200**（与 yml `OUTBOX_POLL_INTERVAL_MS:200` 一致）
- B3 本机开发环境配置.md 补 `OUTBOX_CLEANUP_ENABLED`/`OUTBOX_POLL_INTERVAL_MS` 行

## 四、C 文档滞后回改

- rabbitmq-topology-v1.md L157/L174：多消费者并发消费 → 已落地（Polish-7 M6，2026-08-12）
- 部署安装手册 L21/33/142：Flyway V1-V19 → V1-V26

## 五、提交节奏（每模块一提交）

| 模块 | 提交 | 内容 |
|---|---|---|
| M1 | docs(plan) | 本计划文档 |
| M2 | feat(server) | A：V27 + 配置 + Mapper/Service/Scheduler + 测试 |
| M3 | feat(server) | B：outbox 默认开启 + poll 对齐 |
| M4 | docs | C 回改 + 本机配置文档 + dev-log + 备份留痕 |

## 六、备份与验证边界

- 备份：`local/backup-ops-cleanup/`（原版自基线提取 + SHA-256；local/ 不入库）
- 验证：`mvn test` 全量（541 → ~552+）；可选 `mvn verify -Pmysql-validation`
- 不做：孤儿表处理、历史回填、清理监控、真实 cron 验收；outbox 默认开启影响（30 天前 published 行将被清理）在 dev-log 说明
