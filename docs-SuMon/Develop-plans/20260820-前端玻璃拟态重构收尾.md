# 2026-08-20 前端玻璃拟态重构收尾计划

**状态**：✅ 已收口（2026-08-20）
**前置基线**：main HEAD = `91afbba`
**提交链**：见下方"提交链汇总"（c1 计划 → c2 修复 → c3 补齐 → c4 清理 → c5 主体 → c6 收口）
**承接**：会话 7 遗留 —— `web-vue-SuMon` 下 12 个玻璃拟态样式文件悬置未提交（glass.css 设计系统 + 布局 + 仪表盘 + 列表页）。

## 一、背景与目标

上一会话的玻璃拟态样式重构已完成约 90%，收尾前存在 3 个风险点：

1. **构建级 bug**：`ServerListView.vue` 传给 `listServers` 的字段名是 `order`，而 `ServerQuery` 类型（`src/types/api.d.ts`）只定义 `sort_order` → vue-tsc excess property check 会在 `npm run build` 直接报错。
2. **6 项功能误删**：ServerListView 在重构时被删掉了与样式无关的功能（URL 筛选同步、30s 自动刷新、防抖搜索、SSH 错误码分类提示、删除后末页回退、统一错误解释）。
3. **旧类死代码**：`DashboardAdminCard.vue` 仍用 `dashboard-view__*` 旧类；`DashboardCard.vue` 与 `DashboardServerStatusCard.vue` 是孤儿组件（无生产引用）；`DashboardView.vue` 非 scoped 块残留约 16 个旧样式类，与 liquid-* 新体系并存。

**目标**：修复 + 恢复 + 清理 + 全量验证 + 分模块提交 + 文档收口，使前端玻璃拟态重构成为健康的单一设计系统。

**边界**：本轮只做前端收尾。多实例化阶段二（注册表 Redis + WS 广播 MQ fanout）留到下一轮单独规划。无 REST API / WebSocket 协议变更，OpenAPI 与 Protocol 文档无需同步。无新增 npm 依赖。

## 二、技术栈

Vue 3.5 + TypeScript 5.5 + Element Plus 2.8（@element-plus/icons-vue 2.3）+ Vite 5.4 + vue-tsc 2.1（build/typecheck 校验）+ vue-router 4.4（URL query 同步）+ pinia 3（auth store）+ axios 1.7（apiClient）+ vitest 1.6 + eslint 8.57（--max-warnings 0）+ prettier 3.3。设计系统为原生 CSS 变量 + backdrop-filter，不引入 UI 框架外依赖。

## 三、代码改动（参数传递细节）

### 模块 1：ServerListView 修复 + 功能恢复（c2）

文件：`web-vue-SuMon/src/views/ServerListView.vue`（保持当前玻璃样式不动）

- **修 bug**：`fetchServers()` 参数对象 `order:` → `sort_order:`；统一 `buildQuery(): ServerQuery` 拼参。
- **参数传递**（snake_case 出站，与 OpenAPI 对齐）：
  - `listServers(query: ServerQuery = {})`，字段 `page / page_size / sort_by / sort_order / name / host`（sort_by 白名单 `id|name|host|created_at|updated_at`，sort_order `asc|desc`）
  - `getMetricsHistory(serverId, startISO, endISO, page=1, pageSize=100)`（spark line 7 天）
  - `testSshConnection(serverId)` → `SshTestResult { connected, duration_ms, error_message?, auth_type }`
- **恢复 6 项功能**（从 HEAD `91afbba` 精确移植）：
  1. 防抖搜索：`useDebouncedRef<T>(initial, 500)`（`@/composables/useDebouncedRef`，composable 文件未被删，直接重新导入）；`watch([searchName, searchHost])` + `suppressWatch` + `initialized` 标志防双跑。
  2. URL 同步：`syncQueryToUrl()`（`router.replace({ name: 'servers', query })`，排除默认值不留历史；query 键 `name/host/page/page_size/sort_by/sort_order`）/ `restoreQueryFromUrl()`（`route.query`，`onMounted` 最先调用一次，sort_by 白名单校验）。
  3. 30s 自动刷新：`STATUS_REFRESH_INTERVAL_MS = 30_000` + `startStatusRefresh()`（失败静默保留旧数据）+ `onBeforeUnmount` 清理定时器。
  4. SSH 错误码提示：`ApiBusinessError`（`@/api/client`）+ `ErrorCode`（`@/types/error-code`）switch，9 个 case：SSH_AUTHENTICATION_FAILED / SSH_CONNECTION_TIMEOUT / SSH_HOST_KEY_NOT_CONFIRMED / SSH_HOST_KEY_MISMATCH / SSH_TARGET_FORBIDDEN / SSH_CONNECTION_LIMIT_REACHED / SSH_CONNECTION_FAILED / FORBIDDEN / UNAUTHORIZED。
  5. 删除末页回退：`serverItems.length === 1 && page > 1` 时 `page -= 1` 再 reload。
  6. `explainError()` 统一错误解释（RESOURCE_NOT_FOUND / FORBIDDEN 分类）。
- 顺带修 SSH 列文案 `{{ row.ssh_user }}@:{{ row.ssh_port }}` 的 `@:` 连写问题。

### 模块 2：设计系统补齐（c3）

- `src/styles/glass.css`：新增 `liquid-dot--warn` 变体（琥珀 `#f59e0b` 呼吸圆点，复用 `liquid-pulse` 动效、`--glass-shadow` 体系）。
- `DashboardServersCard.vue`：删 inline style `style="background:#f59e0b;"` → `liquid-dot--warn` 类。
- `DashboardHero.vue`：修 `defineEmits` 结尾 `}>()` 缩进格式。

### 模块 3：旧类清理（c4）

- 引用面确认（已 grep）：`DashboardServerStatusCard` 仅被自身 spec 引用（孤儿）；`DashboardCard` 仅被 `DashboardServerStatusCard` 引用（孤儿）；`DashboardAdminCard` 被 `DashboardView` 引用（功能性，改造）。
- `DashboardAdminCard.vue`：模板换 `el-card.liquid-glass-card` + `liquid-dot--pink` 等新类，样式随组件 scoped 化。
- 删除孤儿：`DashboardCard.vue`、`DashboardServerStatusCard.vue`、`DashboardServerStatusCard.spec.ts`（已备份于 `local/backup-glassmorphism/`）。
- `DashboardView.vue`：删除非 scoped `<style>` 块中不再被引用的旧类（`dashboard-view__card/--badge/--pulse/--card-title/--card-value/--card-hint/--spark-wrap` 等）；保留 scoped 布局类；`availability` 布局中 `:deep(.dashboard-view__card)` 引用随改造一并处理。
- 全仓 grep `dashboard-view__card` 等确认无残留。

### 模块 4：玻璃重构主体（c5）

其余 11 个已完成文件：glass.css（设计系统：品牌色/状态色/玻璃基底/描边/滤镜/投影/微晶反光/容器类/徽标/动效/环境光斑）、global.css（Element Plus 全局玻璃化 + fixed 列透明背景修复 + table-action-group）、MainLayout（侧栏黑曜石玻璃 + 品牌三色发光 + 顶栏玻璃）、PageHeader（微标 + 渐变竖条）、DashboardHero（radial 光斑 + 呼吸光斑）、5 张仪表盘卡片（粉金渐变数字/液体徽标/浅玻璃行）、AlertRecordsView（table-action-group 操作列）。

## 四、备份留痕

`local/backup-glassmorphism/`（本地 exclude，不入 git，基线 `91afbba`）：
- `ServerListView.vue.worktree`（改前工作树版）与 `ServerListView.vue.head-91afbba`（HEAD 版）
- `DashboardView.vue`、`DashboardHero.vue`、`DashboardServersCard.vue`、`DashboardAdminCard.vue`
- `DashboardCard.vue`、`DashboardServerStatusCard.vue`、`DashboardServerStatusCard.spec.ts`（待删文件）
- `BACKUP_MANIFEST.txt` + `BACKUP_MANIFEST.sha256` + `README.md`

## 五、验证与验收标准

- `npm run typecheck` / `npm run build`（vue-tsc --noEmit + vite build）通过
- `npm run lint`（--max-warnings 0）通过
- `npm run test`（vitest）全绿
- 浏览器手测：仪表盘各卡玻璃效果与 hover；服务器列表 F5 恢复筛选 / 30s 自动刷新 / SSH 错误提示 / 删除末页回退；AlertRecords 固定操作列滚动不重叠
- `git log` 含 c1-c6 完整提交链

## 六、提交链汇总

```
c1 docs(plan): glassmorphism frontend closeout plan (20260820)
c2 fix(web): restore server list url-sync/auto-refresh/debounce/ssh-error features, fix sort_order param
c3 style(web): glassmorphism polish - liquid-dot--warn variant, inline style cleanup
c4 refactor(web): migrate legacy dashboard cards to liquid classes, drop dead styles
c5 style(web): glassmorphism design system - glass.css, layout, dashboard cards, list views
c6 docs: glassmorphism frontend closeout (20260820)
```

## 七、风险与注意

- `useDebouncedRef` composable 未被删除，恢复时直接重新导入。
- `restoreQueryFromUrl` 写 searchName 时置 `suppressWatch=true`；watch 仅 `initialized=true` 后触发，避免 onMounted 双跑。
- 删除孤儿组件前已在备份目录留底；vitest 用例数随 spec 删除相应减少（属预期）。
