# 20260805-下一步开发计划（N4 告警增强 / N5 通知可靠性 / N6 运维收尾）

**状态**：已批准，实施中
**日期**：2026-08-05
**前置基线**：`2192044`（N1-N3 收口）

## 一、总览

| 阶段 | 代号 | 类型 | 关键内容 |
|---|---|---|---|
| 告警增强闭环 | **N4** | 功能闭环 | 修正 notify_channels 语义、记录/规则页通知状态展示、监控图告警阈值线（markLine） |
| 通知可靠性二期 | **N5** | 可靠性 | V21 独立通知表 `alert_notifications`、退避重试调度、失败留痕 |
| 运维收尾 | **N6** | 部署/验证 | Docker 资产、首管理员空库并发脚本、DLQ JSON Schema 文档化 |

实施顺序 N4 → N5 → N6；每个功能子模块独立提交 git、改前备份、完成后文档同步。

## 二、N4：告警增强闭环

### N4-1 修正 notify_channels 语义
- `AlertNotificationServiceImpl` 三个 `sendXxx` 改为返回 boolean；`notify()` 仅成功渠道计入 `sentChannels`；全部失败不回写 `notified_at`
- 更新 `AlertNotificationServiceTests`（邮件失败+钉钉成功→只含 dingtalk；全失败→不 updateNotifiedInfo）
- 提交：`fix(server): record only successfully delivered notification channels`

### N4-2 通知状态展示
- 后端：`AlertRecordVo` + `AlertRecordMapper.xml`（resultMap/RecordColumns）+ `openapi-alert.json` 补 `notified_at/notify_channels`
- 前端：`api.d.ts` AlertRecord 补字段；`AlertRecordsView` 加"通知状态"列；`AlertRulesView` 加"通知方式"列
- 提交：`feat(server): expose alert notification status on records` + `feat(web): show notification status on alert record and rule lists`

### N4-3 监控图阈值线（后端零改动）
- `MetricsLineChart.vue` 加 `rules: AlertRule[]` prop，按 metric/enabled/server 匹配挂 markLine（warning 橙 / critical 红虚线）
- `MetricsView.vue` onMounted 拉 `listAlertRules()` 过滤后传入
- `MetricsLineChart.spec.ts` 补 markLine 断言
- 提交：`feat(web): overlay alert threshold lines on metrics charts`

## 三、N5：通知可靠性二期

### N5-1 V21 迁移 `alert_notifications`
每渠道一行：alert_record_id、channel、status(pending/sent/failed)、attempts、next_attempt_at、last_error(500)、时间戳；索引 idx_record_channel、idx_next_attempt

### N5-2 通知服务改造
- `AlertNotificationEntity` + `AlertNotificationMapper`（insert / selectPendingForRetry / markAttempt）
- `AlertNotificationServiceImpl`：插入 pending 行 → 立即尝试 → sent / failed+退避（2^attempts 上限 60s，复用 outbox markRetry 模式）
- `NotificationRetryScheduler`（@Scheduled 30s）：扫 pending 到期行重试，达上限（5 次）置 failed
- `updateNotifiedInfo` 改为至少一渠道 sent 才聚合回写
- 提交：`feat(server): add alert notification attempts with backoff retry (V21)` + `feat(server): retry pending alert notifications on schedule`

### N5-3 前端失败可见性
- `AlertRecordsView` 通知状态列：notify_channels 空但规则配了渠道 → "发送失败"弱提示

## 四、N6：运维收尾

### N6-1 Docker 资产（遵循 .agents/skills/docker-standards）
- `server-java-SuMon/Dockerfile`：maven:3.9.9-eclipse-temurin-21-alpine 构建 → eclipse-temurin:21-jre-alpine 运行，非 root
- `web-vue-SuMon/Dockerfile`：node:20-alpine 构建 → nginx:1.27-alpine，USER nginx
- `agent-go-SuMon/Dockerfile`：golang:1.23-alpine CGO_ENABLED=0 → alpine:3.20，非 root
- 根 `docker-compose.yml`：mysql:8.4 + rabbitmq:4.3-management + server + web + 可选 agent；敏感值 env 占位
- 完善根 `.dockerignore`
- 提交：`chore(deploy): add docker build assets` + `chore(deploy): add docker-compose orchestration`

### N6-2 首管理员空库并发脚本
- `api-test/run-first-admin-concurrency-e2e.ps1` + `verify-first-admin-concurrency.mjs`
- N 并发注册 → 恰好 1 成功为管理员（CAS markAdminInitialized 语义）
- 提交：`test(server): verify first-admin empty-database concurrency`

### N6-3 DLQ 校验补强
- `docs-SuMon/Protocol-SuMon/message-contracts-v1.schema.json`（metrics.reported.v1 JSON Schema 文档化权威源）
- `MetricsReportedMessageSchemaTests`：Jackson 序列化样例与 schema 字段对照（零新依赖）
- 提交：`docs(protocol): add metrics.reported.v1 JSON schema reference` + `test(server): align consumer validation with documented schema`

## 五、实施纪律

1. 备份先行：`C:\Backup\SuSuMonitor\<时间戳>-nX-...`
2. 契约先行：OpenAPI → api.d.ts → 前端
3. 全绿后提交：Maven / vitest / vue-tsc / openapi:check 5/5 / Go
4. 每阶段写开发日志 + 同步 README/04/Use-manual
5. Maven 新依赖走阿里云镜像补本地仓库

## 六、明确不做（后续）

- 通知历史详情弹窗/重试按钮
- 全局默认通知配置 + 设置页
- 真实 SMTP 发送验收、真实联合 E2E 跑通（需凭据，列用户待办）
