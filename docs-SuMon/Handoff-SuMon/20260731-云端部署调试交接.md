# SuSuMonitor 云端部署调试交接（历史记录）

**交接日期**：2026-07-31  
**状态**：历史明文 HTTP 部署曾跑通（前端+后端+Agent 端到端验证）；当前正式部署目标为 HTTPS/WSS，家庭 Linux Agent、真实公网 root PTY 和 Monitor 1011 真实背压仍待专项验收。
**当前校准**：2026-08-27，当前 PR 分支 HEAD `617ccd0`。本文仅用于追溯历史调试过程；历史地址、IP、凭据和命令均已脱敏，文中命令禁止执行。
**适用范围**：前端 Vue 3、后端 Java Spring Boot、Go Agent 的云端+WSL 调试部署

> **安全警告**：本文原始明文公网地址、真实域名、SSH 主机信息及可操作部署命令已脱敏。所有带“历史命令”标记的内容仅保留审计语义，禁止复制、拼接或执行。当前部署请以 `docs-SuMon/Use-manual/Go-Agent 部署使用手册.md` 和 `docs-SuMon/Use-manual/Go-Agent-WSS公网验收手册.md` 为准，并使用受控环境变量和正式 HTTPS/WSS 配置。

> **2026-08-07 历史更新**：系统完成 HTTPS/WSS 迁移。此前的明文 IP、HTTP/WS 地址和一键安装命令已过时，本文件统一脱敏并禁止执行。正式域名不写入仓库，示例统一使用 `SERVER_IP_OR_DOMAIN`。

## 一、服务器环境

| 项 | 值 |
|---|---|
| 云服务器 | OpenCloudOS 9.6（真实主机标识已脱敏） |
| SSH | 主机、用户、密钥路径已脱敏；禁止从本文恢复或执行连接命令 |
| Nginx | 宝塔面板管理，站点路径和监听信息按实际受控环境配置 |
| MySQL | 服务端本机回环访问；库名、用户和密码不记录，密码只从后端进程环境变量读取 |
| Java | OpenJDK 21，systemd 服务 `susumonitor-server.service` |
| 前端 | Nginx 静态文件目录按受控部署环境配置 |

## 二、部署架构

```
浏览器（历史测试网络） ── HTTPS/WSS:443 ──→ nginx（`SERVER_IP_OR_DOMAIN`）
                                           ├── `/` → SPA 静态文件
                                           ├── `/api/` → Java 后端回环端口
                                           ├── `/ws/agent` → Java 后端回环端口（Upgrade）
                                           └── `/ws/monitor` → Java 后端回环端口（Upgrade）
                                                   │
                                     Java 后端 ←── MySQL（均为受控内网/回环访问）
                                                   ↑
                                     WSL Agent（`wss://SERVER_IP_OR_DOMAIN/ws/agent`）
```

- Java 后端仅监听受控回环端口，不直接对公网开放，nginx 是公网入口。
- Agent 从被监控主机出站使用 HTTPS/WSS 连接 `/ws/agent`。
- 浏览器监控订阅走 `/ws/monitor`（带一次性 ticket）。

> 下方原始部署步骤均为历史命令摘要，已删除真实地址、路径和凭据，禁止执行。

## 三、前端部署

### 3.1 构建

```text
历史命令（禁止执行）：在受控环境构建前端并将 dist 产物上传至 Nginx 静态目录；实际主机、用户、路径和认证方式不记录。
```

产物：`dist/index.html` + `dist/assets/*-[hash].js|css`

### 3.2 部署（历史命令，禁止执行）

历史流程为：将 `dist` 产物发布到受控 Nginx 静态目录并调整目录权限。真实主机、账号、路径和上传命令已删除，禁止从本文恢复或执行。

### 3.3 nginx 缓存策略（历史配置摘要）

历史站点配置使用 `index.html` 禁止缓存、哈希静态资源长期缓存，并保留访问日志用于排查资源 404；实际站点路径和配置文件位置不记录。

## 四、后端部署

### 4.1 构建

```bash
cd server-java-SuMon
mvn package -DskipTests    # 产物 target/server-java-SuMon-0.0.1-SNAPSHOT.jar
```

### 4.2 部署（release + symlink 模式）

> 历史部署命令（禁止执行；真实主机、路径和认证信息已删除）：
>
> ```text
> 构建 Java JAR → 上传到受控 release 目录 → 切换 symlink → 重启 systemd 服务
> ```

旧 release 目录保留可回滚（实际回滚动作须按当前运维手册和变更审批执行）。

### 4.3 systemd 服务

历史服务配置仅保留语义：Java JAR 由 `susumonitor-server.service` 管理，异常自动重启，日志进入 journald。具体 `ExecStart`、主机路径和操作命令不写入本文。

### 4.4 后端配置

jar 内 `application.yml`，通过受控环境变量覆盖：
- `DB_HOST`/`DB_PORT`/`DB_NAME`/`DB_USER`/`DB_PASSWORD`（MySQL 连接）
- `CORS_ALLOWED_ORIGINS`（生产使用受控 HTTPS 来源）

实际部署通过 systemd 环境变量或受控启动参数注入；本文不记录进程路径、连接凭据或查询方式。

## 五、Go Agent 部署

### 5.1 一键部署（历史摘要，禁止执行）

历史流程为：通过 HTTPS 下载受控版本的安装脚本，以运行时环境变量注入后端入口和版本，交互式获取管理员凭据，完成登录、预建 server、发放 Token、校验二进制、写入 `agent.env`、安装 systemd 并启动验证。本文不保留可执行管道命令、真实地址或凭据。

**环境变量**：

| `AGENT_BASE_URL` | `https://SERVER_IP_OR_DOMAIN` | 后端地址，生产必须 HTTPS |
| `AGENT_ALLOW_INSECURE_HTTP` | `false` | 生产环境必须保持关闭；历史明文模式禁止执行 |
| `AGENT_VERSION` | `1.0.0` | release 版本号 |
| `AGENT_NAME` | `hostname -s` | 主机显示名 |
| `AGENT_SERVER_ID` | 空（新建） | 已有 server 时复用 |
| `AGENT_ROTATE` | `false` | 是否轮换 token |
| `AGENT_TERMINAL_ENABLED` | `false` | 是否开启 Web 终端 |

> 临时 IPv4 明文测试配置已废弃（`AGENT_ALLOW_INSECURE_HTTP=true`），禁止执行；当前只使用 `AGENT_BASE_URL=https://SERVER_IP_OR_DOMAIN`。

### 5.2 手动部署

```bash
# 构建（交叉编译 linux amd64 静态二进制）
cd agent-go-SuMon
GOOS=linux GOARCH=amd64 CGO_ENABLED=0 go build -o bin/susumonitor-agent-linux-amd64 ./cmd/susumonitor-agent

# 目标机部署
sudo install -m 0755 susumonitor-agent-linux-amd64 /usr/local/bin/susumonitor-agent
sudo mkdir -p /etc/susumonitor /var/log/susumonitor
sudo install -m 0600 agent.env /etc/susumonitor/agent.env    # 填真实值
sudo install -m 0644 susumonitor-agent.service /etc/systemd/system/
sudo systemctl daemon-reload && sudo systemctl enable --now susumonitor-agent
```

### 5.3 Agent 配置

> 历史明文配置（禁止执行）：原 `SUSUMONITOR_BACKEND_URL` 使用 `ws://` 和真实 IP；当前应使用 `wss://SERVER_IP_OR_DOMAIN`，Agent Token 只从受控环境变量注入，不写日志。

### 5.4 WSL 调试机

- WSL2 需 `/etc/wsl.conf` 设 `[boot] systemd=true`（`wsl --shutdown` 后重启生效）
- agent systemd 已 `enable`，WSL 启动后自动跑
- **WSL 不会随 Windows 开机自动启动**（WSL2 设计），需手动打开 WSL 终端或 `wsl` 命令拉起

## 六、本次会话修复的 Bug（历史事实）

| Bug | 根因 | 修复 |
|---|---|---|
| 登录点击无反应 | HTTP 非安全上下文下 `crypto.randomUUID` 不可用，请求拦截器抛错阻断请求 | `client.ts`/`websocket.ts` 加 `typeof crypto.randomUUID==='function'` 守卫 + 降级 |
| 服务器状态显示离线 | `updateAgentHeartbeat` 只更新 `agent_status` 不更新 `status` | `ServerMapper.xml` 三处 SQL 同步 `status` |
| Agent 断开后仍显示在线 | `afterConnectionClosed` 只清内存 registry 不更新 DB | 加 `markOfflineOnDisconnect`（乐观锁 `markAgentOffline`） |
| 终端黑框无响应 | `terminal.open` 的 `message_id` 非 UUID（crypto.randomUUID 降级成 Date.now()-random），后端 validator 拒绝 40003 | `terminal-ws.ts` 用 `getRandomValues` 生成 UUID v4 fallback |
| 终端 cols 极小 | `mountXterm` fit 时容器 CSS 布局未完成，cols=2 | `TerminalView.vue` onSocketReady 前重新 fit |
| 采集时间显示 UTC | `MetricsView` 直接显示 raw ISO 没调 `formatDateTime` | 改用 `formatDateTime` 转本地时区 |

详细记录见当前仓库中的 `docs-SuMon/Difficulty-log/` 与 `docs-SuMon/Develop-log/`；历史问题记录不代表当前部署命令。

## 七、已知问题（历史记录与当前边界）

### 7.1 宽带网络下明文 WebSocket 劫持（历史问题）

历史明文部署中曾观察到 `/ws/monitor` Upgrade 头被剥离并注入脚本；该记录用于说明迁移 HTTPS/WSS 的原因。当前正式目标为 HTTPS/WSS，明文模式禁止执行。

### 7.2 明文 HTTP 安全上下文受限（历史问题）

历史明文页面下部分浏览器 API 受限；当前通过 HTTPS/WSS 正式入口规避该部署边界。

## 八、运维操作索引（历史命令摘要，禁止执行）

### 前端

历史流程：构建前端并将 `dist` 产物发布到受控 Nginx 静态目录。真实主机、账号、路径和上传命令已删除，禁止执行本文历史命令。

### 后端

历史流程：通过受控发布目录切换 JAR 并重启 `susumonitor-server.service`。具体 SSH、路径和 systemd 操作命令已删除，禁止执行。

### Agent（WSL）

历史流程：在 WSL 中检查、重启 Agent 服务并查看日志。具体命令和日志路径不作为当前操作指引，禁止从历史记录恢复执行。

### Nginx

历史流程：测试配置并重载 Nginx，查看访问日志。真实主机、路径和命令已删除，禁止执行。

### MySQL（查指标/状态）

历史流程：通过后端受控环境连接本机数据库查询服务器状态；本文不记录数据库名、用户、密码读取方式或可执行 SQL，禁止执行历史命令。

## 九、预建 Server + 发放 Agent Token（历史接口流程，不执行）

历史流程语义：管理员登录 → 预建 server → 调用 Agent Token register 接口 → 一次性安全接收 Token。本文已删除所有真实地址、示例凭据和可执行 curl 命令；当前请以正式 OpenAPI 契约和运维手册为准。

## 十、文件索引

| 类别 | 路径 |
|---|---|
| 前端源码 | `web-vue-SuMon/src/` |
| 后端源码 | `server-java-SuMon/src/` |
| Agent 源码 | `agent-go-SuMon/` |
| 一键部署脚本 | `agent-go-SuMon/deploy/install-agent.sh`（云端 `/agent/install-agent.sh`） |
| Bug 修复记录 | `docs-SuMon/Difficulty-log/`（历史问题记录） |
| 当前部署手册 | `docs-SuMon/Use-manual/Go-Agent 部署使用手册.md` |
| WSS 公网验收手册 | `docs-SuMon/Use-manual/Go-Agent-WSS公网验收手册.md` |
| 协议文档 | `docs-SuMon/Protocol-SuMon/websocket-protocol.md` |
