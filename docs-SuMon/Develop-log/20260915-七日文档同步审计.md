# 2026-09-15 七日文档同步审计（代码/配置/接口 vs 文档）

- **审计窗口**：2026-09-08 ~ 2026-09-15（最近 7 天，共 56 个提交，自 `b9c153a`/`0751373`（09-12）至 `4dd5cdc`（09-15））
- **审计方法**：以"实现 diff"为事实源，逐项核对公开行为面（协议契约、OpenAPI、REST 端点、Agent 环境变量、前端公开组件行为）与面向运维/调用方的文档（协议 md、openapi json、根 README、agent README、agent `.env.example`）。只记录双侧证据齐全的高置信差异，不做推测；本批次**只改文档与配置样例，不改代码**（伴生的代码缺陷修复见 `docs-SuMon/Bug-fix/2026-09-15-metrics-view-rest-ws-stale-overwrite.md`）。

## 一、窗口内公开行为变更清单（对照基线）

| 变更 | 提交 | 公开行为 |
| --- | --- | --- |
| 协议 v1.4 进程快照 | dd13367/6564c81/948aa01/408230a | `metrics.report` 可选 `process_cpu_top`/`process_mem_top`（≤20 条）；`GET /api/servers/{id}/processes/latest`；`metrics.update` 可选 `processes` 节点 |
| 协议 v1.5 扩展资源 | db464e8/3137e68/5528a2e/57ed0ae | `metrics.report` 可选 `disks`/`nics`（≤32/≤64 条）；`GET /api/servers/{id}/resources/latest`；`metrics.update` 可选 `resources` 节点 |
| openapi-server 0.3.0 | db464e8 | 契约新增 2 端点（18 endpoints） |
| M2 观察期评审报告 | 08730ed/5774d8d/f7e3324/9c9c387 | `GET /api/ai/commands/observation-report`（admin，window_days 1-90 缺省 14，随 `AI_COMMAND_ENABLED` 同生灭）；openapi-command 0.2.0 |
| L2 变更类模板 | cdcb938/14a5fee/d106674 | 模板清单 8→10（`systemctl_reload`/`journalctl_vacuum`，medium）；渲染语义统一为白名单前置的子串替换 |
| Agent 新配置项 | 6564c81、3137e68 | `SUSUMONITOR_PROCESS_TOP_N`（默认 10，0-50）、`SUSUMONITOR_EXTENDED_RESOURCES`（默认 true） |
| Web UX 修复 | 509a193 | 登录失败单提示；路由守卫认证→角色判定顺序 |

## 二、已确认并修复的"行为已变、文档未同步"（高置信）

### 1. Agent `.env.example` 缺失 6 个已实现配置项

- **差异**：`agent-go-SuMon/.env.example` 是运维复制即用的配置模板，但未收录 `internal/config/config.go` 已实现的 6 个环境变量——其中 2 个为窗口内新增且**默认开启**（升级即改变上报内容），4 个为命令域（2026-09-04 `a50cf06` 引入，窗口外，同表顺带补录）：
  - `SUSUMONITOR_PROCESS_TOP_N`（默认 10，0 关闭，上限 50）——窗口内，v1.4
  - `SUSUMONITOR_EXTENDED_RESOURCES`（默认 true，0/false 关闭）——窗口内，v1.5；**默认开启**意味着存量 Agent 升级后自动开始上报 disks/nics，模板缺项使运维无从预判与关闭
  - `SUSUMONITOR_COMMAND_ENABLED`（默认 false）、`SUSUMONITOR_COMMAND_MAX_TIMEOUT_SECONDS`（默认 120）、`SUSUMONITOR_COMMAND_MAX_OUTPUT_BYTES`（默认 65536）、`SUSUMONITOR_COMMAND_RATE_PER_MINUTE`（默认 10，1-600）——窗口外顺带
- **修复位置**：`agent-go-SuMon/.env.example`（NACK 块与 `SUSUMONITOR_LOG_LEVEL` 之间新增 6 行，含逐项默认值注释，取值均逐一核对 `config.go`）
- **依据**：`agent-go-SuMon/internal/config/config.go:179-197`、`:303`（ProcessTopN 0-50 校验）

### 2. Agent README 关键配置表缺失同 6 项

- **差异**：`agent-go-SuMon/README.md` "关键配置项"表宣称覆盖全部配置，终表于 TERMINAL 块，窗口内新增的两个数据采集开关与命令域四项均未收录；表外正文亦未提"多盘/多网卡/Top 进程采集可用环境变量关闭"。
- **修复位置**：`agent-go-SuMon/README.md` 同表插入 6 行（置于 NACK 与 TERMINAL 块之间，与 `.env.example` 顺序一致）
- **依据**：同上；README 原表经 `SUSUMONITOR_[A-Z_]*` 与 config.go 全量差集比对得出（差集恰为这 6 项）

### 3. 根 README 缺失 F3 定时健康报告整块

- **差异**：F3（2026-09-12 落地：`b9c153a` 服务端 + `9a0b9cd` Web 页 + `9d86ced` Android 对接）新增了完整公开行为面——`AiHealthReportScheduler` 定时生成、`GET/POST /api/ai/health-reports*` 三个 admin 端点、`AI_REPORT_ENABLED` 独立 kill switch、V34 `ai_health_reports` 表、`CommandAutoApprovalNotifier` 自动审批事后通知；契约 `openapi-ai.json` 已收录端点，但根 `README.md` 的"AI 能力"行只列到 ⑥，全文 0 次提及"健康报告/F3"。
- **修复位置**：`README.md` AI 能力行插入 ⑦ 段，事实逐项取自 `application.yml`（report 块）、`AiHealthReportScheduler`/`AiHealthReportController`/`CommandAutoApprovalNotifier` 源码与 openapi-ai.json
- **依据**：`grep 健康报告 README.md` 修复前为 0 命中；上述源码文件均存在且开关/默认值已逐一核对

## 三、核对后确认已同步的文档（无需改动）

| 文档 | 结论 |
| --- | --- |
| `docs-SuMon/Protocol-SuMon/websocket-protocol.md` | v1.4/v1.5 增补段落与实现逐项一致：条数上限（20/32/64）、名称长度 128、首周期 nics 缺省、计数回绕按 0、服务器对扩展资源"丢快照不丢核心指标"语义、90 秒窗口、进程字段永久 nack 对比（`:52`、`:54`） |
| `docs-SuMon/Protocol-SuMon/command-protocol-v1.md` | L2 两条模板与子串替换语义已增补（cdcb938），与 Go `command.go`/Java `CommandTemplateRegistry` 逐字一致（正则、argv、天数 1-999） |
| `docs-SuMon/OpenApi-SuMon/openapi-server.json` / `openapi-command.json` | 0.3.0 / 0.2.0 新端点路径、operationId、snake_case 字段与 Controller/VO 一致；`openapi:check` 7/7 含路径漂移校验 |
| 根 `README.md` | 当前基线、AI 能力（①-⑦，⑦ F3 为本审计补录）、Web 前端（批次 2/4）、监控快照面各行均已覆盖窗口内变更；本批次修复待下一批次收口时并入基线行 |
| 开发日志/计划/备份 | 批次 4、部署验收、脏数据清理均有留痕，与提交序列一致 |

## 四、留痕不改的差异（涉及代码侧，需专项决策）

1. **403 不可达**：metrics 面三端点（metrics/latest、processes/latest、resources/latest）的契约与 `@ApiResponses` 均声明 403 "非 admin"，但 `SecurityConfig.java:117` 对 `GET /api/servers/**` 仅要求 `authenticated()`，非 admin 实际可访问（200）。窗口内新增端点沿用了既有文案——双侧自洽地"错"。修正需同步改 Java 注解 + openapi json，且应先确认访问矩阵意图（开放任一认证用户 vs 收紧 admin）。详见 Bug-fix 文档"明确保留不改"节。
2. **`GET /api/ai/commands/observation-report` 的 OpenAPI 注解**未像 metrics 端点那样逐条 `@ApiResponses`（依赖全局错误响应），与命令域其余端点风格一致，不算差异，仅记录。

## 五、本批次产物

| 类型 | 路径 |
| --- | --- |
| 配置样例补录 | `agent-go-SuMon/.env.example` |
| 配置文档补录 | `agent-go-SuMon/README.md` |
| 能力地图补录 | `README.md`（AI 能力行新增 ⑦ F3 段） |
| Bug 修复留痕 | `docs-SuMon/Bug-fix/2026-09-15-metrics-view-rest-ws-stale-overwrite.md` |
| 修改前版本备份 | `docs-SuMon/backup/20260915-竞态修复与文档同步/`（P2-agent 两件、P4-web 两件、P7-docs 一件） |
