# 2026-08-07 Polish-6 M2：告警通知深链到告警 Tab

**状态**：完成（compile + testDebugUnitTest 全绿）
**前置基线**：通知点击仅唤起 App（MainActivity 深链扩展点占位）

## 一、实现

1. **MainViewModel**：新增 `MutableSharedFlow<Unit> openAlertsRequest`（`extraBufferCapacity = 1`，防丢失）与 `requestOpenAlerts()`（`tryEmit`）
2. **MainActivity.handleNotificationIntent**：`ACTION_OPEN_ALERTS` → `mainViewModel.requestOpenAlerts()`（替换原空实现）
3. **AppNavHost**：
   - 新增参数 `openAlertsRequest: Flow<Unit>`（由 MainActivity 注入）
   - `LaunchedEffect` 收集事件 → `navigateToTab(BottomTab.ALERTS)`
   - 抽公共 `navigateToTab(tab)`（popUpTo findStartDestination + launchSingleTop + restoreState，与底部栏点击一致），底部栏 onClick 复用

## 二、参数传递链

```
NotificationHelper.ACTION_OPEN_ALERTS (PendingIntent)
  → MainActivity.onNewIntent/handleNotificationIntent
  → mainViewModel.requestOpenAlerts() [SharedFlow.tryEmit(Unit)]
  → AppNavHost(openAlertsRequest) LaunchedEffect.collect
  → navController.navigate("alerts")
```

## 三、验证

- `:app:compileDebugKotlin` EXIT=0
- `:app:testDebugUnitTest` BUILD SUCCESSFUL
- 无 MainViewModel 相关既有单测受影响

## 四、备份

`local/backup-polish6/M2-deeplink/`：MainActivity.kt、MainViewModel.kt、AppNavHost.kt 原版

## 五、提交

- `feat(android): deep link notification to alerts tab`
