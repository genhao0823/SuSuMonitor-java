# 2026-08-03 云端后端升级至最新代码（8-2 功能全量上线）

**操作**：云端 SuSuMonitor 后端（82.156.245.102，systemd `susumonitor-server`）从 `cloud-20260801-status-realtime`（8-1 构建）升级到 `cloud-20260803`（本地最新代码，412 测试全绿版本）。

## 一、升级内容（8-2 新功能随本次上线）

- 消费失败留痕（`FailedConsumeRecordRecoverer` + `upsertFailed`，failed 行落库）
- 告警逃逸窗口（`confirm_count` 连续越界确认 + **Flyway V18**）
- `/api/admin/users` 泛化（status 筛选分页）+ 批量审核 `failed_ids`
- Agent 在线/离线状态转换推送、Outbox 清理任务（V16/V17 云端已具备，本次 V18 新增）

## 二、操作步骤（遵循升级回滚手册：symlink 版本化 + 可回滚）

1. **本地构建**：`mvn clean package`（412 tests 全绿），jar 54,688,589 字节
2. **云端备份**：
   - 生产库 mysqldump（`--single-transaction --no-tablespaces`）→ `/root/susumonitor-backups/susumonitor-before-v18-20260803-clean.sql`（11.6MB）
   - 现有 jar → `cloud-20260801-status-realtime/*.jar.bak-20260803`
   - 回滚链接 `susumonitor-server.jar.bak.20260803_1014` → 旧版本
3. **部署**：scp 新 jar → `releases/cloud-20260803/`（md5 校验一致）→ symlink 切换 → `systemctl restart`

## 三、验证结果（全过）

| 项 | 结果 |
|---|---|
| 新进程 | MainPID=1599294，2026-08-03 10:16:56 启动（Startup 11.3s） |
| health / ready | 200 / 200 |
| **Flyway V18** | `add alert confirm window` success=1；`alert_rules.confirm_count`（默认 1）、`alert_states.breach_count`（默认 0）列已建 |
| AMQP | `amqp://susumonitor@127.0.0.1:5672/susumonitor` 连接成功 |
| `/api/admin/users` | 端点生效（安全层 401，认证后可用） |
| **失败留痕端到端** | 发布 `cpu_percent=101` 契约非法信封 → `message_consume_records` **failed 行落库**（attempts=1，last_error=`IllegalArgumentException: invalid metrics percentage`）→ DLQ 1 条，purge 清空 |

## 四、回滚方案（如需）

```bash
# 服务端执行
ln -sfn /opt/susumonitor/releases/cloud-20260801-status-realtime/server-java-SuMon-0.0.1-SNAPSHOT.jar /opt/susumonitor/server/susumonitor-server.jar
systemctl restart susumonitor-server
# DB 已应用 V18（加列带默认值，向后兼容，无需回滚）
```

## 五、遗留

- 8-2 功能（/users 分页、批量审核、逃逸窗口、失败留痕）已在云端生效；`verify-admin-batch.mjs` 等脚本对云端新版本后端可直接运行（无需 fallback）
- 验证 admin（cloudval1785683319）已降级无权限残留；测试数据（failed 记录 1 行 + DLQ 已清）保留少量留痕
- 升级窗口（约 30s 重启）内指标上报由 outbox 兜底补发，无数据缺口