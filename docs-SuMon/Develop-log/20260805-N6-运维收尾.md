# 2026-08-05 运维收尾（N6）：Docker 资产 / 首管理员并发验收 / DLQ Schema

**状态**：已实施并提交
**前置基线**：N5 收口

## 一、Docker 资产（遵循 docker-standards）

- 提交：`chore(deploy): add docker build assets for server, web and agent` + `chore(deploy): add docker-compose orchestration`
- `server-java-SuMon/Dockerfile`：maven:3.9.9-eclipse-temurin-21-alpine 多阶段构建 → eclipse-temurin:21-jre-alpine，`adduser` 非 root，仅拷贝 jar
- `web-vue-SuMon/Dockerfile` + `nginx.conf`：node:20-alpine 构建 → nginx:1.27-alpine（USER nginx）；容器内站点 SPA fallback + `/api/`、`/ws/agent`、`/ws/monitor` 反代到 compose `server` 服务（与既有 nginx 示例路径一致）
- `agent-go-SuMon/Dockerfile`：golang:1.23-alpine `CGO_ENABLED=0` 交叉编译 → alpine:3.20 非 root，缓冲目录预建
- 根 `docker-compose.yml`：mysql:8.4 + rabbitmq:4.3-management（数据卷 + healthcheck）→ server（depends_on healthy、环境变量全部 `${VAR:?}` 注入）→ web（`WEB_PORT` 映射）；agent 为可选 profile（systemd 仍是目标主机推荐部署方式）
- 验证：YAML 语法校验通过；本机无 Docker CLI，`docker build`/`docker compose config` 留待有 Docker 环境执行
- 根 `.dockerignore` 此前已较完善，未改动

## 二、首管理员空库并发验收脚本

- 提交：`test(server): verify first-admin empty-database concurrency`
- 新增 `api-test/run-first-admin-concurrency-e2e.ps1`：独立空 schema → 起 Java 服务（18182）→ 调用 verify → 清理（复用 reliable-delivery E2E 的编排模式，环境变量守卫 `SUSUMONITOR_VALIDATION_CONFIRM=FIRST_ADMIN_CONCURRENCY`）
- 新增 `api-test/verify-first-admin-concurrency.mjs`：N（默认 8，2-20）并发 `POST /api/auth/register` → 断言**恰好 1 个成功**且该用户 `GET /api/auth/me` 返回 `role=admin`（覆盖 `UserServiceImpl` 的 `markAdminInitialized` CAS 语义）
- 运行前置：DB 管理员凭据（`SUSUMONITOR_VALIDATION_DB_ADMIN_USER/PASSWORD`）+ `mvn package` 产物

## 三、DLQ 校验补强（文档化 + 对照测试）

- 提交：`docs(protocol): add metrics.reported.v1 JSON schema reference` + `test(server): align consumer validation with documented schema`
- 新增 `docs-SuMon/Protocol-SuMon/message-contracts-v1.schema.json`：`metrics.reported.v1` 的 JSON Schema（draft-07）——信封 required/enum/const + payload 必填 12 字段、百分比 0-100、用量非负；作为消费侧硬编码校验（`MetricsReportedMessageValidator`）的文档化权威源
- 新增 `MetricsReportedMessageSchemaTests`（4 用例，零新依赖）：schema 可解析、required 集合与校验器一致、合法样例序列化满足 schema 字段、缺 `message_id` 样例被校验器拒绝
- 注意：payload 字段顺序为 `cpu, memory, memory_used, memory_total, disk, disk_used, disk_total, net_rx, net_tx, temperature, load_avg`

## 验证矩阵

| 项 | 结果 |
|---|---|
| Maven | **442 tests** 全绿（N4-N6 累计 +24） |
| vitest / vue-tsc | 124 tests / 通过 |
| OpenAPI 契约 | 5/5 |
| Docker | YAML 校验通过；`docker build` 待有 Docker 环境 |

## 备份

- N4：`<时间戳>-n4-alert-closure\N4-before-change`（12 文件）
- N5：`<时间戳>-n5-notification-reliability\N5-before-change`（8 文件）

## 已知边界

- Docker 镜像未实机构建/运行（本机无 Docker CLI）；compose 为"文档 + 语法级"资产，首次实机需调优（如 JVM 内存、健康检查参数）
- 首管理员并发脚本与真实联合 E2E 均需 DB 管理员凭据，属用户侧待办
- schema 对照测试依赖仓库 docs 目录相对路径（surefire cwd 为 server-java-SuMon），移动目录结构需同步
