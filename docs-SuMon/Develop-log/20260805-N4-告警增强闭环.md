# 2026-08-05 告警增强闭环（N4）

**状态**：已实施并提交
**前置基线**：`2192044`（N1-N3 收口）

## 完成内容

### N4-1 修正 notify_channels 语义

- 提交：`08c3458 fix(server): record only successfully delivered notification channels`
- 问题：`notify_channels`/`notified_at` 此前记录"尝试过的渠道"——即使发送抛异常也计入渠道、`notified_at` 照写当前时间
- 修复：`sendEmail/sendDingtalk/sendWebhook` 改为返回 boolean；`notify()` 仅在渠道**实际送达**后计入 `sentChannels`；全部失败时不回写 `notified_at`
- 效果：前端可依据 `notify_channels` 为 null 且 `notified_at` 为 null 识别"通知发送失败"
- 测试：更新 `shouldTolerateChannelFailure`（邮件失败+钉钉成功 → 只含 dingtalk）；新增 `shouldNotWriteNotifiedWhenAllChannelsFail`

### N4-2 通知状态展示

- 提交：`feat(server): expose alert notification status on records` + `feat(web): show notification status on alert record and rule lists`
- 后端：`AlertRecordVo` 加 `notifiedAt/notifyChannels`；`AlertRecordMapper.xml` resultMap/RecordColumns 补两列；两处 `toVo`（记录查询/评估事件）映射；`openapi-alert.json` AlertRecord schema 同步
- 前端：`AlertRecord` 类型补字段；告警记录页新增"通知状态"列（成功渠道 `📧 邮件 / 💬 钉钉 / 🔗 Webhook`，未发送显示"未发送"）；告警规则页新增"通知方式"列（el-tag 展示已配置渠道）
- 验证：Maven 431 tests、vitest 122、契约 5/5

### N4-3 监控图告警阈值线（markLine，后端零改动）

- 提交：`feat(web): overlay alert threshold lines on metrics charts`
- `MetricsLineChart` 新增 `rules` + `serverId` props：图表 key → 规则 metric 映射（cpu_percent→cpu 等，网络指标无对应规则），匹配 `enabled && (server_id 为 null 或等于当前服务器)` 的规则在对应系列画虚线阈值线（warning 橙 / critical 红）
- `MetricsView` onMounted 拉 `listAlertRules()` 过滤 enabled 后传入两个图表
- 测试：新增 2 个 markLine 用例（命中/不匹配忽略、无规则不生成）——vitest **124 tests** 全绿

## 备份

- `C:\Backup\SuSuMonitor\<时间戳>-n4-alert-closure\N4-before-change`（12 文件，SHA256 校验通过）

## 已知边界

- 阈值线按"规则当前配置"绘制；规则在监控页加载后被修改需刷新页面才更新
- `notify_channels` 为 null 时记录页显示"未发送"（含"尝试过但全部失败"与"规则未配置渠道"两种情况，N5 会细化区分）
