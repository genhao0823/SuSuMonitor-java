# 2026-08-02 隔离环境端到端验收（MVP11 + 用户管理 + Agent 重连 + Broker 停机）

**阶段**：低成本快速闭环 A（隔离环境端到端验收）  
**操作人**：genhaosan  
**环境**：MySQL 8.4.9（MySQL84 服务）、RabbitMQ 4.3.4（local/rmq，Erlang 27）、后端 18081 隔离实例（验证库 `susumonitor_metrics_validation`，vhost `susumonitor`）

---

## 一、前置：RabbitMQ 凭据重置

历史 RabbitMQ 密码遗忘（仓库/备份/脚本/PowerShell 历史均无明文）。排查后确认：
- 用户 `susumonitor`（administrator 标签）与 vhost `susumonitor` 均存在（`rabbitmqctl list_users/list_vhosts`）
- **重置**：`rabbitmqctl change_password susumonitor 732682`（rabbitmqctl 本地走节点 RPC 无需旧密码）
- **验证**：Management API 登录 HTTP 200；vhost 队列拓扑完好（`susumonitor.alert.metrics` 等，DLX 参数齐全）

> 教训：凭据应落 `local/server.env`（gitignore）+ 运维手册登记；本次已建临时凭据文件并计划删除。

## 二、验收执行记录（四脚本全 PASS）

| 脚本 | 结果 | 关键断言 |
|---|---|---|
| `verify-mvp11.mjs` | **14 项 PASS（C1-C4）** | 正常消费/幂等/DLQ 分类/**C4 失败留痕** |
| `verify-admin-batch.mjs` | **18 项 PASS（A1-A6）** | 分页/搜索/批量通过/重复批量 failed_ids/批量拒绝/非法 status |
| `verify-go-agent-reconnect.mjs` | **6 checks PASS** | 门禁占满→1s→2s→4s 退避→关闭门禁→认证成功 |
| `verify-mvp11-broker-down.mjs` | **7 项 PASS（B1-B7）** | 停机存活/ready 50301/落库堆积/恢复补发/状态机正确/链路不中断 |

### 1. verify-mvp11（C1-C4）+ DB 侧失败留痕断言

脚本打印 event_id 后，用 SQL 实查 `message_consume_records`：

| event_id | status | attempts | last_error（截断） | consumed_at |
|---|---|---|---|---|
| `25311e46…`（schema_version=2） | **failed** | 1 | AmqpRejectAndDontRequeueException: unsupported schema version | NULL |
| `7854faf3…`（cpu=101） | **failed** | 1 | IllegalArgumentException: invalid metrics percentage | NULL |
| `fcaf0e92…`（正常消息） | consumed | 0 | NULL | 2026-08-02 12:00:05 |

→ **失败留痕（20260802 收口功能）在真实 Broker 环境验证通过**：不可重试数据错误零重试、attempts=1、last_error 落库、正常消息 consumed。

### 2. verify-admin-batch（Sprint5 用户管理接口）

- 分页 `status=pending` total 断言 + `keyword` 精确命中 1 条
- 批量通过 `processed=3 failed=0`；`status=approved` 可见
- 重复批量（已被处理）`processed=0 failed=1` 且 **`failed_ids` 明细包含目标**，无二次业务效果
- 批量拒绝 + `status=rejected` + keyword 搜索精确命中
- 非法 `status=banned` → 40002

### 3. verify-go-agent-reconnect（重连加固验收）

- 按 `run-p2-agent-e2e.ps1` 编排：后端 `AGENT_MAX_CONNECTIONS=3` + `AGENT_MAX_UNAUTHENTICATED_CONNECTIONS=3`，脚本打开 3 个门禁 WS 连接占满名额
- agent 被拒 → **equal jitter 退避 1s→2s→4s**（断言下限 400ms/900ms 放宽 jitter 1/2）
- 关闭门禁 → agent 自动重连并认证成功（`agent authenticated`）
- **前置修正**：`bin/susumonitor-agent.exe` 为 7-25 旧构建（无重连加固日志文案）→ 重新 `go build` 后通过

### 4. verify-mvp11-broker-down（停机消费侧重连，B1-B7 全过）

- B1 基线越界触发 unread → `[ctl] rabbitmq stop`
- B2/B3 停机后 `/api/health` 200、`/api/ready` 503/50301
- B4 停机期间 3 次指标上报**照常落库**（outbox 堆积待补发）
- B5 `[ctl] rabbitmq start` 后业务队列归零（发布器补发 + 消费者自动重连补消费）
- **B6 状态机正确**：停机消息补消费后 1 条 resolved + 1 条新 trigger（continue 不重复建记录）
- B7 恢复后新上报正常评估推送（链路不中断）
- DB 侧：停机期间 3 条消息全部消费（无丢失）

## 三、验收中发现并修正的问题

| 问题 | 根因 | 处置 |
|---|---|---|
| `verify-mvp11.mjs` P0 报 TypeError | B1 泛化 `/users/pending` → `/users?status=pending`（破坏性变更连带） | 脚本适配（commit `1977023`） |
| `verify-mvp11-broker-down.mjs` 同错 | 同上 | 同上 |
| reconnect 断言失败 | `bin/susumonitor-agent.exe` 是旧构建，缺重连日志文案 | 重新 `go build` |
| reconnect gate 失效 | 后端 `AGENT_MAX_CONNECTIONS=1000` 限流放开导致门禁占不满 | 按 ps1 配置 `AGENT_MAX_CONNECTIONS=3` + gate=3 |
| broker-down B4 消息丢失 | 误用 ps1 限流 `AGENT_METRICS_RATE_PER_MINUTE=1`，B1 用掉配额后 report 被限流拒收 | 后端改宽松限流（600/min）后重跑 B1-B7 全过（**非代码 bug**） |

## 四、提交清单（本计划累计）

| Commit | 说明 |
|---|---|
| `d7c1d90` | B1 feat(admin): /users 泛化 + status 筛选 + failed_ids |
| `d22747a` | B2 feat(web): 状态 Tabs + 批量失败明细弹窗 |
| `9b134ba` | B3 test(api-test): verify-admin-batch 验收脚本 |
| `1977023` | test(api-test): 脚本适配 /users 泛化接口 |

## 五、备份与留痕

- `C:\Backup\SuSuMonitor\20260802-sprint5b\`（后端/契约/前端 14 文件 + SHA256SUMS）
- 验收凭据临时文件 `/tmp/val-admin.env`（含验证 admin 账号）——验收结束后**删除**

## 六、环境收尾

- 后端 18081 隔离实例：验收结束后停止
- RabbitMQ：按计划恢复未运行原状（`local/rabbitmq.ps1 stop`）；凭据 `susumonitor / 732682` 已重置，建议登记到 `local/server.env`（gitignore）防再丢
- 验证库残留：验收创建的 server（mvp11bd-*、sprint5_admin_* 用户）留待下次验收复用或按需清理

## 七、遗留 / 诚实边界

- 前端批量失败明细弹窗（B2）与后端 `failed_ids` 为**单元测试覆盖**，本轮端到端脚本断言了 `failed_ids` 明细（A4）——前端 UI 弹窗本身未做浏览器 E2E（ui:e2e 未覆盖该新交互）
- `verify-mvp11-concurrency.mjs`（多消费者并发）仍为扩容前资产，未实跑（单消费者模型，符合当前发布门禁）
- 验证库中历史遗留 pending 用户（非本轮创建）未清理，属验收环境常态