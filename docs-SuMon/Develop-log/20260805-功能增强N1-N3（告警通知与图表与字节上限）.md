# 2026-08-05 功能增强 N1-N3（告警通知 / ECharts 图表 / 队列字节上限）

**状态**：已实施并提交
**前置基线**：`1eecd1c`（M4 文档对齐收口）

## 一、N3：Agent 队列字节上限

- 提交：`d0b17bf feat(agent): add configurable queue byte cap via SUSUMONITOR_METRICS_BUFFER_MAX_BYTES`
- 新增环境变量 `SUSUMONITOR_METRICS_BUFFER_MAX_BYTES`（默认 0=不限制，非 0 须 ≥1024）
- `metricbuffer.Queue` 改为增量维护 `currentBytes`（Enqueue 加、Acknowledge/RejectHead 减），`Stats().PendingBytes` 直接返回计数，避免每次重复序列化
- 字节超限与条数超限一样：拒绝最新采样、`drops++`、保留既有 FIFO 前缀；两种上限独立生效
- 文档：`Go-Agent 部署使用手册` 与 `agent-go-SuMon/README.md` 同步（提交 `0194ada`）
- 验证：`go test ./...` + `CGO_ENABLED=1 go test -race` + `go vet` + `go build` 全 PASS

## 二、N1：告警外部通知渠道（邮件 + 钉钉 + Webhook）

- 提交：
  - V20 迁移：`feat(server): add Flyway V20 alert notification channels schema`
  - 后端：`feat(server): send alert notifications via email, dingtalk and webhook`
  - 前端：`feat(web): add notification config to alert rule dialog`（`d6a2f55`）
  - 文档：Server 部署手册补 `MAIL_*`/`ALERT_*` 环境变量
- 数据库：`V20__add_alert_notification_channels.sql` — `alert_rules` 加 `notify_email/notify_dingtalk/notify_webhook`；`alert_records` 加 `notified_at/notify_channels`
- 后端：
  - 新增 `module/alert/notification/`：`AlertNotificationService(Impl)`（@Async）、`AlertNotificationPublisher`（@TransactionalEventListener AFTER_COMMIT，与 WS 推送并列）、`AlertMessageBuilder`
  - `AsyncConfig`（@EnableAsync + RestTemplate bean）；`pom.xml` 新增 `spring-boot-starter-mail`
  - `AppProperties` 新增 `alert.notification-enabled / mail-from`；`application.yml` 新增 `spring.mail.*`
  - 规则 DTO/VO/Entity/Mapper 全链路携带通知字段；`alert_records` 回写 `notified_at`/`notify_channels`
  - 行为：仅规则存在、启用且配置了至少一个渠道才发送；邮件受总开关 `ALERT_NOTIFICATION_ENABLED` 与 SMTP 配置约束，钉钉/Webhook 直发；单渠道失败只记日志不影响告警主流程
- 前端：规则对话框新增"通知设置（可选）"折叠面板（邮件/钉钉/Webhook 三输入框，创建/编辑回填）；`api.d.ts`、`OpenAPI openapi-alert.json` 契约同步
- 验证：Maven **430 tests** 全绿（新增通知服务/发布器 12 用例）；`npm run openapi:check` 5/5；vitest 通过

## 三、N2：ECharts 指标折线图 + 时间选择器

- 提交：`feat(web): echarts line charts and time range picker for metrics view`
- 新增 `src/components/MetricsLineChart.vue`：ECharts 5.6（此前已装未用）时间轴折线图，支持多指标 series、百分比指标 y 轴固定 0-100、空值过滤、resize/dispose 生命周期
- `stores/metrics.ts`：新增 `timeRange` 状态，`load()` 接受 Date 窗口并按 `collected_at` 升序排序；历史 API `page_size` 默认 500
- `MetricsView.vue`：新增时间范围选择器（快捷 1h/6h/24h/7d）+ 图表/表格 Tabs
- 验证：vue-tsc 通过；vitest **122 tests** 全绿（新增图表组件 4 用例）

## 四、备份

- `C:\Backup\SuSuMonitor\<时间戳>-n3-queue-byte-cap\N3-before-change`（SHA256 校验通过）
- `C:\Backup\SuSuMonitor\<时间戳>-n1-alert-notification\N1-before-change`（18 文件，SHA256 校验通过）
- `C:\Backup\SuSuMonitor\<时间戳>-n2-echarts-metrics\N2-before-change`（SHA256 校验通过）

## 五、已知边界与后续

- 邮件通知未在本机做真实 SMTP 发送验收（需真实 SMTP 账号；单元测试已 mock JavaMailSender 验证消息结构与分发）
- 通知失败仅记日志，无重试/重发机制；`notified_at` 仅在至少一个渠道成功后回写
- 钉钉/Webhook 为同步 POST（@Async 线程内），未做超时与限流配置（RestTemplate 默认）
- 前端图表无"对比上次窗口/导出 CSV"能力（ECharts 已就绪，可作后续增强）
