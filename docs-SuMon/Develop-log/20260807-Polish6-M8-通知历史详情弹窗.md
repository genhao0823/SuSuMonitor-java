# 2026-08-07 Polish-6 M8：Web 通知历史详情弹窗

**状态**：完成（后端 442 测试 + 前端 typecheck/lint/test 124 全绿 + build 成功）
**前置基线**：通知投递明细只在 DB（alert_notifications 表），前端无展示入口

## 一、后端（commit 2445645）

- **新增端点** `GET /api/alerts/records/{id}/notifications`：返回某告警记录各渠道投递记录（channel/status/attempts/next_attempt_at/last_error/created_at/updated_at），记录不存在抛 RESOURCE_NOT_FOUND
- 新增 `AlertNotificationVo`；`AlertNotificationMapper.selectByRecordId` + XML；`AlertRecordService.listNotifications` + 实现（注入 notificationMapper）
- **OpenAPI 同步**：openapi-alert.json 加端点 + `AlertNotification`/`AlertNotificationListResponse` schemas（7 endpoints，契约校验 5/5 通过）
- 后端 442 测试全绿

## 二、前端

- `src/api/alert.ts`：`listAlertRecordNotifications(id)`
- `src/types/api.d.ts`：`AlertNotification` 类型
- `AlertRecordsView.vue`：
  - 操作列加"通知详情"按钮（列宽 120→180，带行级 loading）
  - 新增通知投递历史 ElDialog：渠道/状态/尝试次数/最近错误(带 title 提示)/下次重试/更新时间，空态提示
  - `handleShowNotifications` 加载 + 状态/标签中文映射

## 三、顺带修复

- **既有 lint 回归**（`d22747a` 带入，非本次改动引起）：`AdminUsersView.spec.ts:42` 的 `import()` 类型注解违反 `consistent-type-imports` → 改用 `import type * as ElementPlus`
- **存量 lint warning 清零**：AlertRecordsView.vue / AdminUsersView.vue 的 `max-attributes-per-line`/`attributes-order`/`singleline-html-element-content-newline` 由 `eslint --fix` + 手动修复，**恢复"lint 零警告"基线**（此前 lint 门槛已因存量 warning 不满足）

## 四、验证

- 后端：`mvn test` 442 全绿；OpenAPI 5/5
- 前端：`vue-tsc --noEmit` 通过；`eslint --max-warnings 0` 通过；vitest 124 全绿；`vite build` 成功

## 五、备份

`local/backup-polish6/M8-notification-history/`：后端 AlertRecordController/ServiceImpl 原版

## 六、提交

- `feat(server): alert notification history endpoint`（2445645）
- `feat(web): alert notification history dialog`（本次）
