# 2026-08-02 云端 RabbitMQ 4.3.4 告警链路验收（verify-mvp11 云端实跑）

**前置**：云端 RabbitMQ 已升级 4.3.4 并接入 `susumonitor` vhost（见《20260802-云端RabbitMQ升级4.3.4与配置对齐》）；本机经 SSH 隧道（18080/15672 → 18081/15672）访问云端后端与 Management API。

## 一、执行方式

1. **SSH 隧道**：`ssh -N -L 18081:127.0.0.1:18080 -L 15672:127.0.0.1:15672`（云端后端 + RabbitMQ Management API）
2. **验证 admin**：注册唯一用户 `cloudval1785683319` → 云端 DB 提权（`role='admin', review_status='approved'`，沿用本机验收模式，不触碰云端既有账号）
3. **跑脚本**：`verify-mvp11.mjs`（BASE_URL=18081 默认值经隧道，MANAGEMENT 凭据 susumonitor/732682）

## 二、结果：14 项全 PASS（C1-C4）

| 检查项 | 结果 |
|---|---|
| C1 消息通道正常消费 → alert.push + 告警记录 | ✅ |
| C2 同 event_id 重投幂等（无第二次业务效果） | ✅ |
| C3 非法 JSON / schema_version=2 / 字段契约非法 → DLQ（after=3） | ✅ |
| C4 数据错误拒绝早于业务事务（无告警副作用、队列无堆积） | ✅ |

→ **云端新 broker（4.3.4）+ 云端后端（8-1 构建）的告警链路（outbox 发布 → 消费 → 评估 → alert.push）完整跑通**。

## 三、发现与说明

1. **云端后端为 8-1 旧构建**（`cloud-20260801-status-realtime`）：
   - 无今天的 `/api/admin/users` 泛化端点 → 脚本 P0 增加**版本兼容 fallback**（`/users` 不可用时回退 `/users/pending` 数组），本机新版与云端旧版通用（commit 见仓库日志）
   - 注册规则差异：旧构建用户名**不允许下划线**（`cloudval_xxx` 40002，纯字母数字成功）——新构建允许
   - **无失败留痕功能**（8-2 收口新功能）：C4 的 DB 侧 failed 行在云端不落库（仅有 consumed 行），脚本 C4 核心断言（无副作用）不受影响；云端后端升级到最新代码后 failed 行生效
2. **DB 侧确认（云端生产库）**：正常消息 consumed 仅 1 行（幂等正确）

## 四、清理（已完成）

| 项 | 处置 |
|---|---|
| 临时验证 admin `cloudval1785683319` | 已降级 `role='user', review_status='rejected'`（不再有 admin 权限） |
| 测试服务器（mvp11_alert_consume_*） | 已软删（deleted=1） |
| DLQ 消息（3 条） | 已 purge（0 条） |
| 云端临时文件（凭据/JSON） | 已删除 |
| SSH 隧道 | 已关闭 |

## 五、遗留

- 云端后端仍为 8-1 构建：失败留痕、`/users` 泛化、逃逸窗口控件等 8-2 功能需**云端后端升级到最新代码**后才可用（涉及重新构建 jar + 部署，另行安排）
- 测试规则/告警记录/指标保留在云端生产库（与软删服务器关联，量小，历史记录性质）