# 2026-08-02 云端 RabbitMQ 升级 4.3.4 + 配置对齐本地

**操作**：云端（82.156.245.102）RabbitMQ 由 Docker `rabbitmq:3.13-management` 升级为 `rabbitmq:4.3-management`（4.3.4，与本地同版本线），vhost/账号密码与本地完全一致，云端后端连接配置同步对齐。

## 一、操作记录

1. **预检**：容器数据卷 `f821f894…`→`/var/lib/rabbitmq`、restart=always、端口 5672/15672 全 IP；旧 vhost `/` 队列堆积 **0 消息**（无消息丢失风险）
2. **升级**：`docker pull rabbitmq:4.3-management` → `docker stop/rm rabbitmq` → 以原参数重建（同名/同卷/同端口/restart=always）
3. **数据说明**：新容器节点名变化（`rabbit@e397339aa54a`），旧 3.13 mnesia 数据未随卷迁移（vhost/用户为当日新建的空配置，**零损失**）；4.3.4 初始化出默认 vhost `/` + guest
4. **重建对齐**：`add_vhost susumonitor` + `add_user susumonitor <REDACTED>` + `set_permissions ".*"` + `set_user_tags administrator`；Management API 登录验证 HTTP 200
5. **后端配置对齐**：`/etc/susumonitor/server.env` 追加 `SPRING_RABBITMQ_HOST/PORT/VIRTUAL_HOST/USERNAME/PASSWORD`（备份 `server.env.bak-20260802`）→ `systemctl restart susumonitor-server`

## 二、验证结果（全过）

| 项 | 结果 |
|---|---|
| 云端 broker 版本 | **4.3.4**（与本地一致） |
| Management API（susumonitor/<REDACTED>） | HTTP 200 |
| 云端后端 health / ready | 200 / 200 |
| AMQP 连接日志 | `Created new connection: amqp://susumonitor@127.0.0.1:5672/susumonitor` |
| 队列拓扑 | `susumonitor.alert.metrics` / `.dlq` 由后端自动声明（0 消息） |

## 三、结论

- 本机（local/rmq 4.3.4）与云端（Docker 4.3.4）**版本线统一、vhost/凭据完全一致**，后端配置可互换（仅 host 指向差异）
- 升级前 vhost `/` 队列堆积为 0，无消息丢失；旧 3.13 镜像仍保留在云端（`rabbitmq:3.13-management`），异常可回滚
- 云端后端重启窗口（约 15s）内指标上报由 outbox 兜底（DB 堆积、broker 恢复后补发），无数据缺口

## 四、留痕

- 运维手册环境清单更新（commit 见仓库日志）
- 云端 `server.env.bak-20260802` 备份在 `/etc/susumonitor/`
