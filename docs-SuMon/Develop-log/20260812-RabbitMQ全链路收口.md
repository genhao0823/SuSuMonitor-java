# 开发日志: RabbitMQ 全链路收口

**日期**: 2026-08-13
**操作人**: ZCode
**阶段**: RabbitMQ 全链路收口（M1-M4）

## 背景

alert.triggered.v1 发布 + 消费 + 消息驱动外部通知已先后落地，但 RabbitMQ 链路存在残留：
契约文档状态未同步（2 处"预留/未实现"表述）、DLQ 重放工具只支持 metrics 事件、
alert.triggered 链路无真实 broker 验收记录。本阶段收口：文档清理 + 工具泛化 +
真实链路验收闭环。

## 备份留痕

- `local/backup-rmq-closeout/`（6 个文件，修改前备份）：`api-test/replay-dlq.mjs`、
  `api-test/replay-mvp11-dlq.mjs`、`message-contracts-v1.md`、`rabbitmq-topology-v1.md`、
  `SuSuMonitor-RabbitMQ-运维手册.md`、`README.md`

## 改动内容

### M1 文档残留清理

- `message-contracts-v1.md` §二 表格：`event_type` / `producer` 行更新为
  「当前已实现 metrics.reported 与 alert.triggered」/「metrics-service 与 alert-service」。
- `rabbitmq-topology-v1.md` §十 表格：消费者行改为「已接入（见 §十一）」。
- 新增计划文档 `Develop-plans/20260812-RabbitMQ全链路收口.md`。

### M2 DLQ 工具泛化（`api-test/`）

- `replay-dlq.mjs`（全量处理版）：新增 `--event metrics|alert`（默认 metrics），
  事件 → DLQ/路由键二元组映射（`susumonitor.alert.triggered.dlq` + `alert.triggered.v1`）；
  extractEventId/发布/防循环逻辑不变。
- `replay-mvp11-dlq.mjs`（安全审查版）：同 `--event` 参数；`validateEnvelope()`
  按事件类型校验（alert：event_type=alert.triggered、三 ID 正整数、metric 冻结集），
  不合法 SKIP（不输出 payload）。
- 实测：`--event alert --purge` 成功清空 DLQ（原 2 条 → 0）。

### M3 真实链路验收（`api-test/verify-alert-triggered-chain.mjs`）

新脚本：管理 API 直接发布 alert.triggered.v1 信封，聚焦消费侧全链路：
- P0：管理员登录（首管理员引导后审批提升）、服务器、告警规则（cpu>80 + webhook 渠道）、
  告警记录行（模拟评估器先建 record 再发事件）
- C1：合法信封 → 发布 routed=true → `susumonitor.alert.triggered` 队列归零 →
  `alert_notifications` 排程行落库 → `alert-notifier` 幂等记录 consumed
- C2：同 event_id 重投 → 通知行数不变（幂等零业务效果）
- C3：非法 JSON / schema_version=2 / 非法 metric → DLQ 增量 0→3
- C4：失败留痕落库（failed 行=3）
- 结果：**11/11 PASS**（2026-08-13 实跑，`status:PASS, checks:11`）

验收运行环境：本机 RabbitMQ 4.3.4（local/rabbitmq.ps1）+ MySQL 8.4 +
后端 local profile（18080，密钥用单行 base64 随机值，规避 MSYS 换行/路径转换坑）。

### M4 文档收口

- 运维手册：死信处置补充 `--event metrics|alert` 用法（安全审查版 + 全量处理版）。
- README 已知遗留：replay 工具泛化 / 真实验收完成标记；真实 SMTP 发送仍待外部凭据。
- 本日志 + 计划文档收口。

## 涉及文件

- 修改：`replay-dlq.mjs`、`replay-mvp11-dlq.mjs`、`message-contracts-v1.md`、
  `rabbitmq-topology-v1.md`、`SuSuMonitor-RabbitMQ-运维手册.md`、`README.md`
- 新建：`verify-alert-triggered-chain.mjs`、`Develop-plans/20260812-RabbitMQ全链路收口.md`
- 验证：`message_consume_records` 复核
  `consumer='alert-notifier' GROUP BY status → consumed 3 / failed 3`

## 验证

- 验收脚本 11/11 PASS（上述 C1-C4）。
- OpenAPI 契约校验 5/5（pre-commit 自动执行，无 REST 变更）。
- 提交：M1-M4 共 4 个 commit。
- 环境复原：后端进程已停（如需可重启动）、RabbitMQ 保持运行。
- 待办留痕：真实 SMTP 发送验收（需真实邮箱凭据）；`alert.resolved.v1`（恢复事件）
  契约仍为勘察项（拓扑文档只预留命名空间，未定义）。