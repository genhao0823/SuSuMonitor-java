# 2026-08-03 Agent 指标可靠投递（ACK 确认与离线缓冲）开发记录

## 完成内容

### M1：Server 入口确认

- 提交：`773fc4d feat(server): acknowledge agent metric delivery`
- `metrics.report` 在 `MetricsService.report(...)` 成功返回后回传 `metrics.ack`。
- ACK 使用原请求 `message_id`，payload 返回 `server_id`、`collected_at`。
- 失败路径只返回既有 `error`，不会误发 ACK；重复 ID 的既有入口幂等逻辑无需新增 migration。
- WebSocket 协议更新至 v1.2，明确 ACK 不代表 RabbitMQ、Monitor 推送或告警消费完成。

### M2：Agent 有界持久化 FIFO

- 提交：`7db29b6 feat(agent): buffer metrics until acknowledged`
- 新增 `internal/metricbuffer`：完整 `metrics.report` 外层帧以 JSON snapshot 原子写入；每次变更使用临时文件、`Sync`、rename。
- Reporter 先入队、后发送；只允许一个 in-flight 指标，只有匹配队首 ID 的 `metrics.ack` 才持久化删除并发送下一条。
- 断线或 ACK 丢失后，认证完成回调会用原 UUID 重放队首，服务端入口幂等处理重复上报。
- 新增 `SUSUMONITOR_METRICS_BUFFER_PATH` 与 `SUSUMONITOR_METRICS_BUFFER_MAX_ENTRIES`，安装脚本创建受限目录。
- 队列满时拒绝最新采样、保留现存 FIFO；损坏/不匹配 spool 启动失败，避免静默数据损失。

### M3：ACK 超时重传、重放节流与抖动

- 提交：`d2a9311 feat(agent): retry metrics after acknowledgement timeout`
  - 新增 ACK deadline、`initial → max` 指数退避；deadline 后保留 spool 队首，以原完整帧和 UUID 重传。
  - 认证/断线/ACK 均使旧 delivery generation 和 timer 失效，避免陈旧 timer 误发。
- 提交：`a41d07c feat(agent): pace queued metric replay`
  - 新增 backlog ACK 后的最小发送间隔，默认 2500ms，避免恢复时打满 Server metrics 限流。
- 提交：`0933872 feat(agent): jitter metric retry backoff`
  - 新增可配置 equal jitter；默认启用，实际 retry 等待为退避时间的 1/2–1 倍。
- 新增环境配置：ACK timeout、retry initial/max、jitter 开关、backlog replay 最小间隔；链路为 env → `config.Config` → `main.go` → `reporter.Options`。
- 协议明确：未收到 ACK 的 Agent 可重发不变的完整 `metrics.report`，且必须复用原 `message_id`。

## 修改前备份与留痕

| 阶段 | 路径 | 内容 |
|---|---|---|
| M1 | `C:\Backup\SuSuMonitor\20260803-140000-agent-reliable-delivery\M1-before-change` | Server WebSocket、测试、协议文档（4 文件） |
| M2 | `C:\Backup\SuSuMonitor\20260803-140000-agent-reliable-delivery\M2-before-change` | Agent WebSocket、Reporter、Config、启动、部署及测试（10 文件） |
| M3 | `C:\Backup\SuSuMonitor\20260803-140000-agent-reliable-delivery\M3-before-change` | Agent/root README、总规划、协议文档（4 文件） |
| M3 retry | `C:\Backup\SuSuMonitor\20260803-150000-agent-metrics-retry\M3-1-before-change` | ACK deadline、配置、样例、启动和测试（9 文件） |
| M3 pacing | `C:\Backup\SuSuMonitor\20260803-150000-agent-metrics-retry\M3-2-before-change` | 重放节流相关代码、配置、样例和测试（9 文件） |
| M3 jitter | `C:\Backup\SuSuMonitor\20260803-150000-agent-metrics-retry\M3-2b-before-change` | retry jitter 相关代码、配置、样例和测试（9 文件） |
| M3 docs | `C:\Backup\SuSuMonitor\20260803-150000-agent-metrics-retry\M3-3-before-change` | README、协议、规划与开发记录（5 文件） |

每个备份目录包含 `SHA256SUMS.txt` 与 `FILES.txt`；创建后均已读取抽查目标文件及清单非空。

## 实际验证

| 命令/范围 | 结果 |
|---|---|
| `mvn test -Dtest=AgentWebSocketHandlerTests` | PASS，10 tests |
| `mvn test -Dtest=AgentWebSocketHandlerTests,MetricsServiceTests,AlertMessageConsumerTests` | PASS，24 tests |
| `mvn test` | PASS，414 tests |
| `go test ./...` | PASS |
| `go vet ./...` | PASS |
| `go build ./cmd/susumonitor-agent` | PASS |
| `CGO_ENABLED=1 go test -race ./...` | PASS |
| pre-commit `npm run openapi:check` | PASS，5/5 OpenAPI files |

首次从仓库根目录错误执行 Maven，因根目录没有 `pom.xml` 失败；随即切换至 `server-java-SuMon/` 后执行的相关测试与全量测试均通过。首次 Go 测试暴露旧 Reporter 构造参数与 Windows 测试绝对路径不兼容，已补齐 queue 依赖和 `t.TempDir()` 绝对路径测试后全量通过。

## 已知边界与后续

- 本次没有在真实隔离环境连接 M1 Server，不能宣称断网/重启联合 E2E 已完成。
- 当前 Server 没有 `metrics.nack`；非认证失效的普通 `error` 不会删除队首，保持安全但可能阻塞后续样本，后续需定义可重试/不可重试错误策略。
- 当前未实现队列字节上限、ACK 超时主动重发、恢复期节流及管理端积压展示；这些应拆为后续独立模块。
- 发布顺序必须先 Server M1，再部署 Agent M2；不能将 M2 Agent 连接到不支持 `metrics.ack` 的旧 Server。
