# 2026-09-16 七日文档同步审计（代码/配置/接口 vs 文档）

- **审计窗口**：2026-09-09 ~ 2026-09-16（最近 7 天，自 `45f508b` 前后（09-15 02:45）至 `de99916`（09-16 06:54），共约 40 个提交；重点覆盖 24 小时窗口 `dd8a897..de99916` 的逐文件读码）
- **审计方法**：以「实现 diff + 本机实测」为事实源，逐项核对公开行为面（REST 端点、OpenAPI 契约、WebSocket 协议、Agent 环境变量、验收脚本调用契约）与面向运维/调用方的文档（协议 md、openapi json、根 README、Use-manual、agent `.env.example`/README、`susumonitor.http`）。只记录双侧证据齐全的高置信差异，不做推测。
- **边界**：本审计批次**只改文档**；窗口内发现的代码缺陷（验收脚本未适配批次 8 令牌）已由代码提交 `ce68d79` 单独闭环，档案见 `Bug-fix/2026-09-16-first-admin-scripts-bootstrap-40310.md`。

## 一、窗口内公开行为变更清单（对照基线）

| 变更 | 提交 | 公开行为 | 文档面核对结果 |
| --- | --- | --- | --- |
| 批次 8 首管理员一次性初始化令牌 | 9eef0ff/55ea1b9/2f9f124/4df0bc9/70b3201/de99916 | 空库注册强制 `bootstrapToken`（40310/40311）；新公开端点 `GET /api/auth/bootstrap-status`；V37 三列；`AUTH_BOOTSTRAP_TOKEN` 预置（32-128，fail-fast） | openapi-auth 0.3.0、OpenAPI README、根 README、部署手册 §五点五、.env.example、docker-compose 均已同步 ✅；**api-test 验收脚本与 susumonitor.http 未同步**（代码缺陷，ce68d79 修复） |
| 批次 6 metrics 访问矩阵收口 | 3a1716c/5ca5a36/bca9819 | openapi-server 0.3.1：metrics 面五个端点删除不可达 403 声明（任一认证用户可读） | OpenAPI README 漂移行与统计已同步 ✅；Use-manual/README 无「仅 admin 可读指标」残留表述 ✅ |
| 批次 7 隔离库 MySQL IT 复跑 | 2a98362/af48e4d/26d8fa0 | 测试侧修复（V33 缺省/ODKU 返回值/TINYINT 布尔/聚合断言），无公开行为变化 | Bug-fix 档案与开发日志已落档 ✅ |
| 批次 2 web 快照竞态修复 | 31d997a/d78d009 | MetricsView 进程/资源卡片 REST 初载不再覆盖 WS 已推送快照 | `Bug-fix/2026-09-15-metrics-view-rest-ws-stale-overwrite.md` 已留档 ✅ |
| （窗口外顺带核对）协议 v1.4/v1.5、M2 评审报告、L2 模板 | 09-15 批次 | 见 `20260915-七日文档同步审计.md` | 上轮审计四项修复复核在位（agent `.env.example` 六变量、agent README、README F3、Go 缓存处置）✅ |

## 二、已核对一致（双侧证据，无需变更）

1. **OpenAPI 统计与内容**：7 份契约 JSON 实测合计 50 路径 / 59 端点操作，与 OpenApi README 逐文件统计一致；`openapi-auth.json` 0.3.0 的 `bootstrap-status`（GET，公开）、`RegisterRequest.bootstrapToken`（可选，minLength 32 / maxLength 128，writeOnly）、register 403 响应与 `AuthController`/`RegisterRequest` 注解一致，`npm run openapi:check` 7/7 实测通过。
2. **websocket-protocol v1.4/v1.5**：进程快照（≤20 条）与扩展资源（disks ≤32 / nics ≤64）、90 秒新鲜窗、不落库、v1.5 非法整组丢弃 + 告警不阻断核心链路的描述，与 agent `ExtendedSampler`/server 注册表/web 卡片实现一致。
3. **部署手册 §五点五 首次初始化令牌**：三种取令牌途径（自动生成横幅 / `AUTH_BOOTSTRAP_TOKEN` 预置 / 持 AES_GCM_KEY 应急解密）、32-128 与启动 fail-fast、重启未消费重新打印、消费即失效，逐条与 `BootstrapTokenServiceImpl`/`CredentialCipher` 对应；§六 验收清单 Flyway 范围已写 V1-V37。
4. **根 README 质量基线**：874/874（server）、229/229 + 五门禁（web）、128/128（Android）、openapi:check 7/7 均在本机实测复跑确认；「已知遗留」与缺陷清单的首管理员条目已按批次 8 收口改写。
5. **agent 配置文档**：`.env.example` 与 agent README 的六个环境变量补录在位；`SUSUMONITOR_PROCESS_TOP_N`/`SUSUMONITOR_EXTENDED_RESOURCES` 与 `internal/config` 默认值/取值范围一致。
6. **`api-e2e-test.mjs` 引用**：README 提及的该文件实际位于 `web-vue-SuMon/scripts/api-e2e-test.mjs`（git 历史可追溯），非悬空引用。

## 三、确认并修复的「行为已变、文档未同步」（高置信）

### 1. Go-Agent 部署使用手册 §五 缺两个采集开关

- **差异**：§五 的 agent.env 模板与「关键配置项」表未收录 `SUSUMONITOR_PROCESS_TOP_N`（v1.4，默认 10，0 关闭，上限 50）与 `SUSUMONITOR_EXTENDED_RESOURCES`（v1.5，**默认开启**，0/false 关闭）。两者均为窗口内（09-15 批次 4）新增且直接改变升级后的上报内容；后者的默认开启意味着存量 Agent 升级即开始上报多盘/多网卡数据，这份 Use-manual 是运维部署时的配置面，缺项使预判与关闭无从下手。09-15 审计补录了 agent-go 仓库内 `.env.example` 与 README，漏了本手册。
- **修复位置**：`docs-SuMon/Use-manual/Go-Agent 部署使用手册.md` §五——agent.env 模板末尾两行 + 关键配置项表两行（说明含默认值与关闭方式）。
- **依据**：`agent-go-SuMon/internal/config/config.go`（ProcessTopN 0-50 校验、ExtendedResources 默认 true）与 `20260915-七日文档同步审计.md` §二.1 的同一事实源。

### 2. 根 README 基线行过期

- **差异**：「当前基线」仍写 `main @ 2f9f124`，未包含批次 8 收口（de99916）与其后的验收脚本修复（ce68d79）。
- **修复位置**：`README.md` 基线表「当前基线」行，改为 `ce68d79` 并补一句本轮修复摘要。

### 3. OpenAPI README 漂移台账缺批次 8 行

- **差异**：「漂移状态」引述块最新条目为批次 6（openapi-server 0.3.1），批次 8 的 openapi-auth 0.2.0→0.3.0（新增 bootstrap-status 端点 + bootstrapToken 可选字段 + register 403）未在台账留痕——文件清单与 auth 端点表虽已更新，但台账作为「何时、为何漂移」的因果账缺一行。
- **修复位置**：`docs-SuMon/OpenApi-SuMon/README.md` 漂移状态块追加批次 8 行。

## 四、未复核项（如实记录）

- Android 真机行为、隔离库 MySQL IT（`RUN_MYSQL_VALIDATION_TESTS` 门控）与真实空库 MySQL E2E 编排器全链路：本机环境不具备（Docker Desktop 未运行），相关 IT 随批次 9 复跑；脚本侧修复已通过静态检查与 mock 三路冒烟验证。
