# 2026-09-16 批次 7：隔离库 MySQL IT 复跑专项

**模块**：隔离库 MySQL IT 全量复跑 + 四类 IT 测试侧欠账清偿
**实施分支**：`main`
**前置基线**：`bca9819`（批次 6 收口）；计划文档提交 `2a98362`
**性质**：验证专项 + 测试侧修复，产品代码（src/main）、契约、前端、Agent 零变更。

## 一、本轮结论

在一次性隔离 MySQL 8.4.11 容器（`susumonitor-it-mysql-20260916`，回环 13307→3306，随机 root 密码仅经 shell 环境变量传递不落盘）中全量复跑 `**/*IT.java`：

- **首轮**（基线 `2a98362`）：surefire 单测 **855/855** 全绿；failsafe IT **55 例中 3 Failures + 8 Errors**，集中于 `AiCommandRunMySqlValidationIT`（9 红）与 `AiHealthReportMySqlValidationIT`（2 红）——按计划 P2 口径停批复跑，立 Bug-fix 档案并转入修复。
- **修复后全量复跑**：surefire **855/855** + failsafe **55/55（0 Failures 0 Errors 0 Skipped）**，BUILD SUCCESS（01:20 min）；`flyway_schema_history` 36 行 `success=1`（最高 V36），业务表 23 张。
- 四类根因全部为 **IT 测试文件自身欠账**（V33 实体字段缺省 / ODKU 返回值 H2 语义 / TINYINT(1) 布尔映射 / 聚合断言自相矛盾），产品代码零缺陷，详见 `Bug-fix/2026-09-16-mysql-it-四类欠账修复.md`。

## 二、容器与环境参数（留痕，凭据不落盘）

```text
docker run -d --name susumonitor-it-mysql-20260916 \
  -e TZ=Asia/Shanghai -e MYSQL_ROOT_PASSWORD=<48位随机hex,现场生成> \
  -e MYSQL_DATABASE=susumonitor_it_validation_20260916 \
  -p 127.0.0.1:13307:3306 mysql:8.4
# 就绪：mysqladmin ping 约 15s OK；VERSION()=8.4.11

IT 执行环境变量（shell export，不入档不入 Git）：
RUN_MYSQL_VALIDATION_TESTS=true
DB_HOST=127.0.0.1  DB_PORT=13307
DB_NAME=susumonitor_it_validation_20260916   # 过 TargetDatabaseGuard 三重校验
DB_USER=root       DB_PASSWORD=<同容器root密码>
APP_ENV=it-validation
JWT_SECRET / AES_GCM_KEY=<test profile 同款非生产测试密钥>

执行命令：cd server-java-SuMon && ./mvnw verify -Pmysql-validation
```

数据源链路：shell export → JVM 环境变量 → Spring `systemEnvironment` PropertySource → `application.yml:11` 的 `${DB_*}` 占位符 → HikariDataSource；门控链：`@EnabledIfEnvironmentVariable` + 各 IT 类 `TargetDatabaseGuard`（本机 host + 隔离库名）双保险。

## 三、四类根因摘要（详情见 Bug-fix 档案）

| # | 根因 | 表现 | 修复 |
|---|---|---|---|
| 1 | `newRun` 未同步 V33 NOT NULL 审计列 | 8 Errors（`Column 'risk_level' cannot be null`） | helper 补 `riskLevel="low"`/`approvalMode="manual"` + 落库断言补读两列 |
| 2 | ODKU 返回值按 H2 语义断言 | 1 Failure（expected 1 but was 2） | 对齐项目内既有惯例断言 `>= 1` + 注释 |
| 3 | TINYINT(1) 强转 Number | 1 Error（ClassCastException）+ 1 潜伏 | 新增 `tinyintValue` helper 收敛 Boolean/Number 两态 |
| 4 | 聚合断言与语义/数据矛盾 | 2 Failures（窗口样本落在窗内；max_disk 断言软删行值） | 窗外行显式 createdAt + 窗内样本断言 sum=7；断言 70→50 与软删排除语义对齐 |

修复仅触碰 2 个 IT 测试文件；修改前版本备份至 `backup/20260916-批次7-隔离库IT复跑/P2-server/`（SHA-256 manifest 同存）。

## 四、验证门禁执行记录

1. 首轮 `./mvnw verify -Pmysql-validation`（EXIT=1）：surefire 855/855 + failsafe 55 例 3F/8E——失败面完整定位并归因。
2. 修复后同命令全量复跑（EXIT=0，BUILD SUCCESS，01:20 min）：surefire **855/855** + failsafe **55/55（0/0/0）**，11 类全执行、0 skip。
3. 数据库侧复核：`flyway_schema_history` 36 行 `success=1`（V1→V36）；`information_schema.tables` 业务表 23 张。
4. 本批次零前端/Agent/契约改动，前端五门禁与 Agent test/vet 不重跑（口径：批次 6 已全绿留痕）。

## 五、备份与留痕

| 文件/对象 | 位置 | 说明 |
|---|---|---|
| `README.md`（修改前） | `docs-SuMon/backup/20260916-批次7-隔离库IT复跑/P3-docs/README.md` | SHA-256 `7dc7bea9…cc463c`，manifest 同存 |
| `AiCommandRunMySqlValidationIT.java`（修改前） | `docs-SuMon/backup/20260916-批次7-隔离库IT复跑/P2-server/` | SHA-256 `d1406cda…46d0b` |
| `AiHealthReportMySqlValidationIT.java`（修改前） | 同上 | SHA-256 `f45941d3…41fd22` |
| 容器 | `susumonitor-it-mysql-20260916` | 收口后 `docker rm -f`，无 volume 残留 |

本日志不记录任何口令/密钥明文。

## 六、README 质量基线回填

根 README「质量基线」区新增批次 7 条目：IT 55/55（首轮暴露 11 红已修复）、单测 855/855、Flyway V36 全量迁移验证；第 20 行 AI 长条目内「10 个 MySQL IT 文件 45 例待隔离库复跑」为 2026-09-07 历史快照不改写，已由本批全量收口。

## 七、提交序列

- `2a98362` docs(develop-plans)：批次 7 计划落档
- fix(test)：2 个 IT 文件四类欠账修复（本批）
- docs：Bug-fix 档案 + 开发日志 + README 回填 + 计划勾选（本批）
- chore(ops)：容器清理复核（并入 docs 或独立，以实际提交为准）
