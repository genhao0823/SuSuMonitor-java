# 2026-08-07 Polish-6 M4：服务器列表排序选择器

**状态**：完成（compile + testDebugUnitTest 全绿）
**前置基线**：sortBy/sortOrder 状态与 onSortChange 已有，UI 无入口

## 一、实现

- `ServerListScreen` 搜索栏下新增 `SortSelector` 组件：两个 `ExposedDropdownMenuBox`
  - **排序字段**：默认(ID)/名称/主机/状态/创建时间/更新时间（与后端 `SORT_BY_WHITELIST` 对齐）
  - **排序方向**：升序/降序（`asc`/`desc`）
- 选择后调 `viewModel.onSortChange(sortBy, sortOrder)`（复用已有方法，触发重拉第一页）
- 字段选项硬编码为与后端白名单一致的列表，避免非法值被后端拒

## 二、参数传递

```
SortSelector(onSortChange = viewModel::onSortChange)
  → ServerListViewModel.onSortChange(sortBy: String, sortOrder: String)
  → loadFirstPage() → fetchPage → GET /api/servers?sortBy=..&sortOrder=..
```

## 三、验证

- `:app:compileDebugKotlin` + `:app:testDebugUnitTest` BUILD SUCCESSFUL

## 四、备份

`local/backup-polish6/M4-sort-ui/`：ServerListScreen.kt 原版

## 五、提交

- `feat(android): server list sort selector`
