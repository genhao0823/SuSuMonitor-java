<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.susumonitor.server.module.alert.consume.ConsumeRecordCleanupMapper">

    <delete id="deleteExpiredBatch">
        DELETE FROM message_consume_records
        WHERE id IN (
            SELECT expired_records.id
            FROM (
                SELECT id
                FROM message_consume_records
                WHERE created_at &lt; #{cutoffTime}
                ORDER BY id
                LIMIT #{batchSize}
            ) expired_records
        )
    </delete>
</mapper>
