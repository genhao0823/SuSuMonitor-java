<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.susumonitor.server.module.ai.mapper.AiDiagnosticRunMapper">
    <insert id="insertRun" useGeneratedKeys="true" keyProperty="run.id" keyColumn="id">
        INSERT INTO ai_diagnostic_runs
        (request_id, correlation_id, actor_id, server_id, prompt_version, provider, model, status,
         context_hash, created_at)
        VALUES
        (#{run.requestId}, #{run.correlationId}, #{run.actorId}, #{run.serverId}, #{run.promptVersion},
         #{run.provider}, #{run.model}, #{run.status}, #{run.contextHash}, #{run.createdAt})
    </insert>
    <update id="completeRun">
        UPDATE ai_diagnostic_runs
        SET status = #{run.status}, result_json = #{run.resultJson}, input_tokens = #{run.inputTokens},
            output_tokens = #{run.outputTokens}, total_tokens = #{run.totalTokens},
            duration_ms = #{run.durationMs}, completed_at = #{run.completedAt}
        WHERE id = #{run.id}
    </update>
    <update id="failRun">
        UPDATE ai_diagnostic_runs
        SET status = #{run.status}, error_code = #{run.errorCode}, duration_ms = #{run.durationMs},
            completed_at = #{run.completedAt}
        WHERE id = #{run.id}
    </update>
    <delete id="deleteExpiredBatch">
        DELETE FROM ai_diagnostic_runs
        WHERE created_at &lt; #{cutoffTime}
        ORDER BY id
        LIMIT #{batchSize}
    </delete>
</mapper>
