<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.susumonitor.server.module.metrics.outbox.OutboxCleanupMapper">
    <!-- 双层派生表避免 MySQL 删除目标表同时读取的 1093 限制。 -->
    <delete id="deletePublishedBeforeBatch">
        DELETE FROM message_outbox
        WHERE id IN (
            SELECT expired_outbox.id
            FROM (
                SELECT id
                FROM message_outbox
                WHERE status = 'published'
                  AND published_at &lt; #{cutoffTime}
                ORDER BY published_at ASC, id ASC
                LIMIT #{batchSize}
            ) expired_outbox
        )
    </delete>
</mapper>
